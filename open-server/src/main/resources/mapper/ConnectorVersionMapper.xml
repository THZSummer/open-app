<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.it.works.wecode.v2.modules.connector.mapper.ConnectorVersionMapper">

    <!-- ResultMap -->
    <resultMap id="BaseResultMap" type="com.xxx.it.works.wecode.v2.modules.connector.entity.ConnectorVersion">
        <id column="id" property="id"/>
        <result column="connector_id" property="connectorId"/>
        <result column="connection_config" property="connectionConfig"/>
        <result column="create_time" property="createTime"/>
        <result column="last_update_time" property="lastUpdateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="last_update_by" property="lastUpdateBy"/>
    </resultMap>

    <!-- Base Column -->
    <sql id="Base_Column_List">
        id, connector_id, connection_config, create_time, last_update_time, create_by, last_update_by
    </sql>

    <!-- Insert -->
    <insert id="insert" parameterType="com.xxx.it.works.wecode.v2.modules.connector.entity.ConnectorVersion">
        INSERT INTO openplatform_v2_cp_connector_version_t (
            id, connector_id, connection_config, create_time, last_update_time, create_by, last_update_by
        ) VALUES (
            #{id}, #{connectorId}, #{connectionConfig}, #{createTime}, #{lastUpdateTime}, #{createBy}, #{lastUpdateBy}
        )
    </insert>

    <!-- Select By ConnectorId -->
    <select id="selectByConnectorId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM openplatform_v2_cp_connector_version_t
        WHERE connector_id = #{connectorId}
        LIMIT 1
    </select>

    <!-- Update -->
    <update id="update" parameterType="com.xxx.it.works.wecode.v2.modules.connector.entity.ConnectorVersion">
        UPDATE openplatform_v2_cp_connector_version_t
        SET connection_config  = #{connectionConfig},
            last_update_time   = #{lastUpdateTime},
            last_update_by     = #{lastUpdateBy}
        WHERE id = #{id}
    </update>

    <!-- Delete By ConnectorId -->
    <delete id="deleteByConnectorId">
        DELETE FROM openplatform_v2_cp_connector_version_t
        WHERE connector_id = #{connectorId}
    </delete>

</mapper>