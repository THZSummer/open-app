<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.it.works.wecode.v2.modules.app.mapper.AppPropertyMapper">

    <!-- ResultMap -->
    <resultMap id="BaseResultMap" type="com.xxx.it.works.wecode.v2.modules.app.entity.AppProperty">
        <id column="id" property="id"/>
        <result column="parent_id" property="parentId"/>
        <result column="property_name" property="propertyName"/>
        <result column="property_value" property="propertyValue"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="last_update_time" property="lastUpdateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="last_update_by" property="lastUpdateBy"/>
    </resultMap>

    <!-- Select By AppId And PropertyName -->
    <select id="selectByAppIdAndPropertyName" resultMap="BaseResultMap">
        SELECT id, parent_id, property_name, property_value, status,
               create_time, last_update_time, create_by, last_update_by
        FROM openplatform_app_p_t
        WHERE parent_id = #{appId}
          AND property_name = #{propertyName}
          AND status = 1
        LIMIT 1
    </select>

</mapper>
