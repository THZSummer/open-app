<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xxx.api.approval.mapper.ApprovalLogMapper">

    <resultMap id="BaseResultMap" type="com.xxx.api.approval.entity.ApprovalLog">
        <id column="id" property="id"/>
        <result column="record_id" property="recordId"/>
        <result column="node_index" property="nodeIndex"/>
        <result column="level" property="level"/>
        <result column="operator_id" property="operatorId"/>
        <result column="operator_name" property="operatorName"/>
        <result column="action" property="action"/>
        <result column="comment" property="comment"/>
        <result column="create_time" property="createTime"/>
        <result column="last_update_time" property="lastUpdateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="last_update_by" property="lastUpdateBy"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, record_id, node_index, level, operator_id, operator_name,
        action, comment, create_time, last_update_time, create_by, last_update_by
    </sql>

    <!-- 插入审批日志 -->
    <insert id="insert" parameterType="com.xxx.api.approval.entity.ApprovalLog">
        INSERT INTO openplatform_v2_approval_log_t (
            id, record_id, node_index, level, operator_id, operator_name,
            action, comment, create_time, last_update_time, create_by, last_update_by
        ) VALUES (
            #{id}, #{recordId}, #{nodeIndex}, #{level}, #{operatorId}, #{operatorName},
            #{action}, #{comment}, #{createTime}, #{lastUpdateTime}, #{createBy}, #{lastUpdateBy}
        )
    </insert>

</mapper>
