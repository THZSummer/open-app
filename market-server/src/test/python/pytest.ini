[pytest]
testpaths = .
python_files = test_*.py
markers =
    L0: 冒烟测试 — 基础连通性 (每次commit)
    L1: 核心CRUD — 增删改查 (PR门禁)
    L2: 生命周期 — 状态流转/审批 (每日回归)
    L3: 端到端 — 部署启动调用全链路 (每周)
    L4: 边界反向 — 异常/校验/极端场景 (发布前)
timeout = 60
addopts = -v --tb=short
