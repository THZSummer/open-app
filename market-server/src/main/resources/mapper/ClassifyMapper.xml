<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.it.works.wecode.v2.modules.lookup.mapper.ClassifyMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.xxx.it.works.wecode.v2.modules.lookup.entity.ClassifyEntity">
        <id column="classify_id" property="classifyId"/>
        <result column="classify_code" property="classifyCode"/>
        <result column="classify_name" property="classifyName"/>
        <result column="path" property="path"/>
        <result column="classify_desc" property="classifyDesc"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="last_update_time" property="lastUpdateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="last_update_by" property="lastUpdateBy"/>
    </resultMap>

    <!-- 基础列 -->
    <sql id="Base_Column_List">
        classify_id, classify_code, classify_name, path, classify_desc, status,
        create_time, last_update_time, create_by, last_update_by
    </sql>

    <!-- 插入分类 -->
    <insert id="insert" parameterType="com.xxx.it.works.wecode.v2.modules.lookup.entity.ClassifyEntity">
        INSERT INTO openplatform_lookup_classify_t (
            classify_id, classify_code, classify_name, path, classify_desc, status,
            create_time, last_update_time, create_by, last_update_by
        ) VALUES (
            #{classifyId}, #{classifyCode}, #{classifyName}, #{path}, #{classifyDesc}, #{status},
            #{createTime}, #{lastUpdateTime}, #{createBy}, #{lastUpdateBy}
        )
    </insert>

    <!-- 根据ID查询分类 -->
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM openplatform_lookup_classify_t
        WHERE classify_id = #{classifyId}
    </select>

    <!-- 根据分类编码和路径查询分类 -->
    <select id="selectByCodeAndPath" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM openplatform_lookup_classify_t
        WHERE classify_code = #{classifyCode}
          AND path = #{path}
    </select>

    <!-- 查询分类列表 -->
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM openplatform_lookup_classify_t
        <where>
            <if test="classifyCode != null and classifyCode != ''">
                AND classify_code LIKE #{classifyCode}
            </if>
            <if test="classifyName != null and classifyName != ''">
                AND classify_name LIKE #{classifyName}
            </if>
            <if test="classifyDesc != null and classifyDesc != ''">
                AND classify_desc LIKE #{classifyDesc}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC, classify_id DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 统计分类总数 -->
    <select id="countList" resultType="long">
        SELECT COUNT(*)
        FROM openplatform_lookup_classify_t
        <where>
            <if test="classifyCode != null and classifyCode != ''">
                AND classify_code LIKE #{classifyCode}
            </if>
            <if test="classifyName != null and classifyName != ''">
                AND classify_name LIKE #{classifyName}
            </if>
            <if test="classifyDesc != null and classifyDesc != ''">
                AND classify_desc LIKE #{classifyDesc}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <!-- 更新分类 -->
    <update id="update" parameterType="com.xxx.it.works.wecode.v2.modules.lookup.entity.ClassifyEntity">
        UPDATE openplatform_lookup_classify_t
        SET classify_name = #{classifyName},
            path = #{path},
            classify_desc = #{classifyDesc},
            <if test="status != null">
                status = #{status},
            </if>
            last_update_time = #{lastUpdateTime},
            last_update_by = #{lastUpdateBy}
        WHERE classify_id = #{classifyId}
    </update>

    <!-- 删除分类 -->
    <delete id="deleteById">
        DELETE FROM openplatform_lookup_classify_t
        WHERE classify_id = #{classifyId}
    </delete>

</mapper>
