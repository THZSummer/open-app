<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxx.it.works.wecode.v2.modules.sdk.mapper.PermissionMapper">

    <!-- ==================== selectByCategoryAlias ==================== -->
    <select id="selectByCategoryAlias" resultType="com.xxx.it.works.wecode.v2.modules.sdk.vo.SdkPermissionVO">
        SELECT p.id, p.name_cn AS nameCn, p.name_en AS nameEn, p.scope
        FROM openplatform_v2_permission_t p
        INNER JOIN openplatform_v2_category_t c ON p.category_id = c.id
        WHERE c.category_alias = #{categoryAlias}
          AND p.status = 1
        ORDER BY p.id ASC
    </select>

    <!-- ==================== countByIdAndCategoryAlias ==================== -->
    <select id="countByIdAndCategoryAlias" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM openplatform_v2_permission_t p
        INNER JOIN openplatform_v2_category_t c ON p.category_id = c.id
        WHERE p.id = #{id}
          AND c.category_alias = #{categoryAlias}
          AND p.status = 1
    </select>

</mapper>
