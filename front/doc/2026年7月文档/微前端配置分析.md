# qiankun 微前端工程配置分析

## 一、工程总览

| 工程 | 技术栈 | 构建工具 | 端口 | host 域名 | 角色 |
|------|--------|---------|------|----------|------|
| main-app | React 18 | Webpack 5 | 8080 | localhost.uat.com | 主应用（基座） |
| sub-app-b | React 18 | Vite 5 | 5174 | localhost.uat.com | 子应用 B |
| sub-app-c | React 18 | Webpack 5 | 8082 | localhost.uat.com | 子应用 C |
| sub-app-d | Vue 3 | Webpack 5 | 8083 | localhost.uat.com | 子应用 D |
| sub-app-e | Vue 3 | Vite 5 | 5175 | localhost.uat.com | 子应用 E |

> **说明：** 所有工程统一使用 `localhost.uat.com` 作为开发服务器 host，而非默认的 `localhost`。这样做便于跨子应用的 cookie / 域名隔离调试，同时避免某些浏览器对 `localhost` 的特殊安全策略干扰 qiankun 资源加载。

## 二、五个工程的关系

```
┌──────────────────────────────────────────────────────┐
│                main-app (主应用/基座)                   │
│                React + Webpack :8080                  │
│                host: localhost.uat.com                │
│                                                        │
│  ┌──────────┐  ┌───────────────────────────────────┐ │
│  │ 侧边导航   │  │  #sub-app-viewport (挂载容器)       │ │
│  │ /        │  │                                   │ │
│  │ /qiankun/sub-b ─┤  -> sub-app-b 渲染于此          │ │
│  │ /qiankun/sub-c ─┤  -> sub-app-c 渲染于此          │ │
│  │ /qiankun/sub-d ─┤  -> sub-app-d 渲染于此          │ │
│  │ /qiankun/sub-e ─┤  -> sub-app-e 渲染于此          │ │
│  └──────────┘  └───────────────────────────────────┘ │
└──────────────────────────────────────────────────────┘
        │ fetch entry          │ fetch entry          │ fetch entry          │ fetch entry
        ▼                      ▼                      ▼                      ▼
┌───────────────┐     ┌───────────────┐     ┌───────────────┐     ┌───────────────┐
│ sub-app-b     │     │ sub-app-c     │     │ sub-app-d     │     │ sub-app-e     │
│ React + Vite  │     │ React+Webpack │     │ Vue3 +Webpack │     │ Vue3 + Vite   │
│ :5174         │     │ :8082         │     │ :8083         │     │ :5175         │
│ localhost.uat │     │ localhost.uat │     │ localhost.uat │     │ localhost.uat │
└───────────────┘     └───────────────┘     └───────────────┘     └───────────────┘
```

**关系说明：**

- **main-app** 是基座（主应用），负责侧边导航、路由分发、子应用注册与生命周期管理
- 主应用使用 **HashRouter**，所有子应用的 `activeRule` 统一以 `/qiankun/` 为前缀（如 `/qiankun/sub-b`），通过 hash 匹配激活
- 四个子应用各自独立运行在独立端口，彼此不依赖，互相不知道对方存在
- 主应用通过 qiankun 的 `registerMicroApps` 注册子应用，根据浏览器 URL hash 自动匹配 `activeRule`，拉取对应子应用的 entry 资源并渲染到 `#sub-app-viewport` 容器中
- 四个子应用也可脱离主应用独立运行（直接访问各自端口），得益于各自的独立运行检测逻辑
- 所有工程共享同一个自定义域名 `localhost.uat.com`，需在系统 hosts 文件中添加 `127.0.0.1 localhost.uat.com`

---

## 三、主应用（main-app）的微前端配置

### 3.1 安装 qiankun 依赖

在 `package.json` 中引入：

```json
"qiankun": "^2.10.5"
```

**作用：** 提供 `registerMicroApps`、`start` 等 API，是整个微前端框架的核心库。

### 3.2 子应用注册列表（microApps.js）

```js
/**
 * 生成基于 hash 的 activeRule 匹配函数
 * @param {string} rule - 路由规则，如 '/qiankun/sub-b'
 * @returns {function} 返回匹配函数，供 qiankun 判断子应用是否激活
 */
function hashRule(rule) {
  return (location) => location.hash.startsWith('#' + rule);
}

const microApps = [
  {
    // 子应用 B（React + Vite）
    name: 'sub-app-b',
    entry: '//localhost.uat.com:5174',
    container: '#sub-app-viewport',
    activeRule: hashRule('/qiankun/sub-b')
  },
  {
    // 子应用 C（React + Webpack）
    name: 'sub-app-c',
    entry: '//localhost.uat.com:8082',
    container: '#sub-app-viewport',
    activeRule: hashRule('/qiankun/sub-c')
  },
  {
    // 子应用 D（Vue3 + Webpack）
    name: 'sub-app-d',
    entry: '//localhost.uat.com:8083',
    container: '#sub-app-viewport',
    activeRule: hashRule('/qiankun/sub-d')
  },
  {
    // 子应用 E（Vue3 + Vite）
    name: 'sub-app-e',
    entry: '//localhost.uat.com:5175',
    container: '#sub-app-viewport',
    activeRule: hashRule('/qiankun/sub-e')
  }
];
```

| 字段 | 作用 |
|------|------|
| `name` | 子应用唯一标识，必须与子应用导出的名称一致，qiankun 据此匹配 |
| `entry` | 子应用资源入口地址（含自定义域名和端口），qiankun 会 fetch 这个地址拉取 HTML 和 JS |
| `container` | 子应用挂载的 DOM 容器选择器，所有子应用共用 `#sub-app-viewport` |
| `activeRule` | **函数形式**，从 `location.hash` 中匹配路由前缀。主应用使用 HashRouter，qiankun 默认匹配 `location.pathname`（始终为 `/`），因此必须改为函数从 hash 中匹配 |

> **关键点：为什么 activeRule 用函数而非字符串？**
>
> 主应用使用 `HashRouter`，URL 形如 `http://localhost.uat.com:8080/#/qiankun/sub-b`。此时 `location.pathname` 始终为 `/`，qiankun 默认的字符串匹配方式无法工作。改用函数形式，通过 `location.hash.startsWith('#/qiankun/sub-b')` 判断子应用是否激活。

### 3.3 注册并启动 qiankun（index.jsx）

```js
import { registerMicroApps, start } from 'qiankun';
import { createRoot } from 'react-dom/client';
import React from 'react';
import App from './App';
import microApps from './microApps';

/**
 * 注册所有子应用
 * qiankun 会根据 activeRule 自动加载对应子应用并渲染到 container
 */
registerMicroApps(microApps);

/**
 * 启动 qiankun
 * prefetch  开启子应用预加载，提升切换速度
 * sandbox   开启样式隔离，避免主子应用样式互相污染
 */
start({
  prefetch: true,
  sandbox: { experimentalStyleIsolation: true }
});

// 渲染主应用根组件
const root = createRoot(document.getElementById('root'));
root.render(<App />);
```

| 配置 | 作用 |
|------|------|
| `registerMicroApps(microApps)` | 将子应用配置注册到 qiankun，qiankun 内部监听路由变化 |
| `start()` | 启动 qiankun 框架 |
| `prefetch: true` | 在浏览器空闲时预加载子应用资源，切换时更快 |
| `sandbox.experimentalStyleIsolation` | 开启实验性样式隔离，给子应用样式加 `div[data-qiankun]` 前缀作用域，防止主子应用 CSS 互相污染 |

### 3.4 主应用布局与路由（App.jsx）

```jsx
import React from 'react';
import { HashRouter, Routes, Route, Link } from 'react-router-dom';
import './index.css';

/**
 * 主应用布局组件
 * 包含侧边导航栏和内容区域
 * 使用 HashRouter，qiankun 子应用从 location.hash 中匹配 activeRule
 */
function App() {
  return (
    <HashRouter>
      <div className="layout">
        {/* 侧边导航栏 */}
        <aside className="sidebar">
          <h1 className="logo">微前端基座</h1>
          <nav>
            <Link to="/" className="nav-link">首页</Link>
            <Link to="/qiankun/sub-b?source=main-app&userId=1001" className="nav-link">子应用 B</Link>
            <Link to="/qiankun/sub-c?source=main-app&userId=1001" className="nav-link">子应用 C</Link>
            <Link to="/qiankun/sub-d?source=main-app&userId=1001" className="nav-link">子应用 D</Link>
            <Link to="/qiankun/sub-e?source=main-app&userId=1001" className="nav-link">子应用 E</Link>
          </nav>
        </aside>
        {/* 内容区域：主应用自身页面 + 子应用挂载容器 */}
        <main className="content">
          {/* 主应用自身路由页面 */}
          <Routes>
            <Route path="/" element={<div className="home">主应用首页</div>} />
            {/* qiankun 子应用路由占位，实际渲染由 #sub-app-viewport 容器接管 */}
            <Route path="qiankun/*" element={null} />
          </Routes>
          {/* 子应用挂载容器：qiankun 根据 activeRule 自动把对应子应用渲染到这里 */}
          <div id="sub-app-viewport"></div>
        </main>
      </div>
    </HashRouter>
  );
}

export default App;
```

| 配置 | 作用 |
|------|------|
| `<HashRouter>` | 主应用使用 HashRouter，URL 以 `#/` 路由。这是 activeRule 必须用函数匹配 hash 的根本原因 |
| `<Link to="/qiankun/sub-b?...">` | 导航链接统一以 `/qiankun/` 为前缀，可附带查询参数传递给子应用 |
| `<Route path="qiankun/*" element={null} />` | 路由占位符。匹配 qiankun 子应用路由时主应用自身不渲染内容（element 为 null），实际渲染由 `#sub-app-viewport` 容器接管 |
| `<div id="sub-app-viewport"></div>` | 子应用挂载容器，对应 `microApps.js` 中 `container: '#sub-app-viewport'` |

### 3.5 Webpack 配置（webpack.config.js）

```js
module.exports = {
  // ...其他配置
  devServer: {
    // 使用自定义域名，便于跨子应用 cookie / 域名隔离
    host: 'localhost.uat.com',
    port: 8080,
    historyApiFallback: true,
    hot: true
  }
};
```

| 配置 | 作用 |
|------|------|
| `host: 'localhost.uat.com'` | 使用自定义域名而非 `localhost`，便于跨子应用 cookie / 域名隔离 |
| `port: 8080` | 主应用端口 |
| `historyApiFallback: true` | 支持 HashRouter 下的历史路由回退 |

---

## 四、子应用 B（sub-app-b, React + Vite）的微前端配置

Vite 子应用的接入方式与 Webpack 子应用完全不同，使用 `vite-plugin-qiankun` 插件，不需要手动导出 UMD 生命周期。

### 4.1 安装 vite-plugin-qiankun

在 `package.json` 中引入：

```json
"vite-plugin-qiankun": "^1.0.15"
```

**作用：** Vite 的构建产物是 ESM 格式，qiankun 默认无法直接获取生命周期钩子。此插件在 Vite 构建时注入适配层，使 qiankun 能识别并接管 Vite 子应用。

> **注意：** 虽然 `package.json` 中也安装了 `@vitejs/plugin-react`，但 `vite.config.js` 中并未启用它。这是因为 React 的 `react-refresh` preamble 会与 qiankun 沙箱的 `window` 不一致产生冲突，改用 esbuild 内置的 JSX 转换来规避此问题。

### 4.2 Vite 配置（vite.config.js）

```js
import { defineConfig } from 'vite';
import qiankun from 'vite-plugin-qiankun';

// Vite 子应用接入 qiankun 配置
// 说明：不使用 @vitejs/plugin-react，改用 esbuild 内置的 JSX 转换（automatic runtime），
//       从根源避免 react-refresh preamble 与 qiankun 沙箱的 window 不一致问题
export default defineConfig({
  plugins: [
    // 注入子应用名称，qiankun 据此识别并接管子应用生命周期
    qiankun('sub-app-b', { useDevMode: true })
  ],
  // 使用 esbuild 的 automatic JSX runtime 转换 JSX（React 18+），
  // 不注入 react-refresh，无 preamble、无 HMR 检测代码
  esbuild: {
    jsx: 'automatic'
  },
  server: {
    // 使用自定义域名，便于跨子应用 cookie / 域名隔离
    host: 'localhost.uat.com',
    port: 5174,
    // 允许主应用跨域拉取子应用资源
    cors: true,
    origin: 'http://localhost.uat.com:5174'
  }
});
```

| 配置 | 作用 |
|------|------|
| `qiankun('sub-app-b', ...)` | 第一个参数是子应用名称，必须与主应用 `microApps.js` 中的 `name` 一致。`useDevMode: true` 开启开发模式适配 |
| `esbuild.jsx: 'automatic'` | 使用 React 18+ 的 automatic JSX runtime，不使用 `@vitejs/plugin-react`。避免 `react-refresh` preamble 与 qiankun 沙箱 `window` 不一致的冲突 |
| `server.host: 'localhost.uat.com'` | 使用自定义域名，与主应用保持一致 |
| `server.cors: true` | 允许主应用（localhost.uat.com:8080）跨域请求子应用（localhost.uat.com:5174）的资源 |
| `server.origin` | 固定静态资源的 origin 为子应用地址。Vite 默认会用相对路径，被主应用加载时会导致资源 404，必须固定为绝对地址 |

### 4.3 入口文件生命周期导出（main.jsx）

```jsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import { BrowserRouter, HashRouter } from 'react-router-dom';
import { renderWithQiankun, qiankunWindow } from 'vite-plugin-qiankun/dist/helper';
import App from './App';

// 子应用根实例引用
let root = null;

/**
 * 渲染子应用到挂载节点
 * @param {Object} props qiankun 注入的参数对象（可含 container 等）
 */
function render(props) {
  // 解构出挂载容器
  const { container } = props || {};
  // 被主应用加载时用 container 内的 #root，独立运行时用 document 的 #root
  const mountNode = container ? container.querySelector('#root') : document.getElementById('root');
  // 根据运行环境设置路由 basename，qiankun 环境下用 activeRule 作为 basename
  const isQiankun = qiankunWindow.__POWERED_BY_QIANKUN__;
  const basename = isQiankun ? '/qiankun/sub-b' : '/';
  // qiankun 环境下使用 HashRouter，从 hash 解析路由
  const Router = isQiankun ? HashRouter : BrowserRouter;
  root = createRoot(mountNode);
  root.render(
    <Router basename={basename}>
      <App />
    </Router>
  );
}

// 通过 vite-plugin-qiankun 导出生命周期，并兼容独立运行
renderWithQiankun({
  /**
   * bootstrap：子应用首次激活时调用一次
   */
  bootstrap() {
    console.log('子应用 B bootstrap');
  },
  /**
   * mount：每次进入子应用时调用
   * @param {Object} props qiankun 注入的参数对象
   */
  mount(props) {
    render(props);
  },
  /**
   * unmount：每次离开子应用时调用
   * @param {Object} props qiankun 注入的参数对象
   */
  unmount(props) {
    if (root) {
      root.unmount();
      root = null;
    }
  },
  update() {}
});

// 独立运行（非 qiankun 环境）时直接渲染
if (!qiankunWindow.__POWERED_BY_QIANKUN__) {
  render({});
}
```

| 配置 | 作用 |
|------|------|
| `renderWithQiankun()` | `vite-plugin-qiankun` 提供的辅助函数，替代手动 `export` 生命周期。内部将 `bootstrap/mount/unmount` 注册到 qiankun 能识别的全局变量上 |
| `qiankunWindow.__POWERED_BY_QIANKUN__` | Vite 子应用不能直接访问 `window.__POWERED_BY_QIANKUN__`（沙箱隔离），需通过 `qiankunWindow` 获取。用于判断是否被主应用加载 |
| `container` | 被主应用加载时，qiankun 传入 `container`（即 `#sub-app-viewport` 的 DOM），子应用在其内部查找 `#root` 挂载；独立运行时直接用 `document.getElementById('root')` |
| `basename` | qiankun 环境下设为 `/qiankun/sub-b`（与主应用 activeRule 一致），独立运行时为 `/` |
| `Router` 切换 | qiankun 环境下用 `HashRouter`（与主应用一致，从 hash 解析路由），独立运行时用 `BrowserRouter` |

> **注意：Vite 子应用不需要 `public-path.js`**，因为 `vite-plugin-qiankun` 和 `server.origin` 配置已解决了资源路径问题。

---

## 五、子应用 C（sub-app-c, React + Webpack）的微前端配置

Webpack 子应用采用 qiankun 官方推荐的手动导出生命周期 + UMD 打包方式。

### 5.1 public-path.js 动态资源路径

```js
/**
 * 动态设置 publicPath
 * 被主应用加载时，qiankun 会注入子应用资源地址，避免静态资源加载 404
 * 注意：此文件必须在入口最前面引入
 */
if (window.__POWERED_BY_QIANKUN__) {
  // eslint-disable-next-line no-undef
  __webpack_public_path__ = window.__INJECTED_PUBLIC_PATH_BY_QIANKUN__;
}
```

**作用：** Webpack 的 `__webpack_public_path__` 是全局变量，用于动态设置静态资源加载的基础路径。独立运行时为默认值（`/`）；被主应用加载时，qiankun 会注入 `__INJECTED_PUBLIC_PATH_BY_QIANKUN__`（值为子应用 entry 地址），确保子应用的图片、字体、chunk 等资源从子应用自己的服务器加载，而不是主应用服务器，避免 404。

### 5.2 Webpack 配置 UMD 导出 + CORS

```js
module.exports = {
  output: {
    // 导出为 umd 格式，供主应用 qiankun 获取子应用导出的生命周期
    library: 'sub-app-c',
    libraryTarget: 'umd',
    // 避免与其它子应用的 chunk 加载全局变量冲突
    chunkLoadingGlobal: 'webpackJsonp_sub_app_c'
  },
  devServer: {
    // 使用自定义域名，便于跨子应用 cookie / 域名隔离
    host: 'localhost.uat.com',
    port: 8082,
    // 允许主应用跨域拉取子应用资源
    headers: {
      'Access-Control-Allow-Origin': '*'
    },
    historyApiFallback: true,
    hot: true
  }
};
```

| 配置 | 作用 |
|------|------|
| `libraryTarget: 'umd'` | 将打包产物导出为 UMD 格式，挂载到 `window['sub-app-c']` 上。qiankun 主应用通过 `import-html-entry` 拉取子应用 JS 后，从该全局变量上获取 `bootstrap/mount/unmount` 生命周期函数 |
| `library: 'sub-app-c'` | UMD 库的名称，必须与主应用注册的 `name` 一致 |
| `chunkLoadingGlobal` | Webpack 按需加载 chunk 时使用的全局变量名。多个子应用默认都用 `webpackJsonp`，会互相覆盖，必须改为唯一名称 |
| `host: 'localhost.uat.com'` | 使用自定义域名，与主应用保持一致 |
| `headers: CORS` | 主应用（8080）需要 fetch 子应用（8082）的 JS 资源，属于跨域，必须设置 `Access-Control-Allow-Origin: *` |

### 5.3 入口文件导出生命周期（main.jsx）

```jsx
// 必须在最前面引入，用于动态设置 publicPath
import './public-path';
import React from 'react';
import { createRoot } from 'react-dom/client';
import { BrowserRouter, HashRouter } from 'react-router-dom';
import App from './App';

// 子应用根实例引用
let root = null;

/**
 * 渲染子应用到挂载节点
 * @param {Object} props qiankun 注入的参数对象（可含 container 等）
 */
function render(props) {
  // 解构出挂载容器
  const { container } = props || {};
  // 被主应用加载时用 container 内的 #root，独立运行时用 document 的 #root
  const mountNode = container ? container.querySelector('#root') : document.getElementById('root');
  // 根据运行环境设置路由 basename，qiankun 环境下用 activeRule 作为 basename
  const isQiankun = window.__POWERED_BY_QIANKUN__;
  const basename = isQiankun ? '/qiankun/sub-c' : '/';
  // qiankun 环境下使用 HashRouter，从 hash 解析路由
  const Router = isQiankun ? HashRouter : BrowserRouter;
  root = createRoot(mountNode);
  root.render(
    <Router basename={basename}>
      <App />
    </Router>
  );
}

// 独立运行（非 qiankun 环境）时直接渲染
if (!window.__POWERED_BY_QIANKUN__) {
  render({});
}

/**
 * bootstrap：子应用首次激活时调用，只执行一次
 */
export async function bootstrap() {
  console.log('子应用 C bootstrap');
}

/**
 * mount：每次进入子应用时调用
 * @param {Object} props qiankun 注入的参数对象
 */
export async function mount(props) {
  render(props);
}

/**
 * unmount：每次离开子应用时调用
 * @param {Object} props qiankun 注入的参数对象
 */
export async function unmount(props) {
  if (root) {
    root.unmount();
    root = null;
  }
}
```

| 配置 | 作用 |
|------|------|
| `import './public-path'` 置顶 | 必须在所有其他模块之前执行，确保 Webpack 的 `publicPath` 在任何资源加载前就被正确设置 |
| `window.__POWERED_BY_QIANKUN__` | qiankun 在 `mount` 前注入的全局变量，值为 `true` 表示当前被主应用加载。独立运行时为 `undefined`，走 `render({})` 独立渲染 |
| `basename` | qiankun 环境下设为 `/qiankun/sub-c`，独立运行时为 `/` |
| `Router` 切换 | qiankun 环境下用 `HashRouter`，独立运行时用 `BrowserRouter` |
| `export bootstrap/mount/unmount` | qiankun 要求的三个生命周期函数，通过 UMD 导出后挂到 `window['sub-app-c']` 上，主应用通过 `import-html-entry` 获取 |

### 5.4 三个生命周期的作用

| 生命周期 | 调用时机 | 作用 |
|---------|---------|------|
| `bootstrap` | 子应用首次激活时调用一次 | 初始化全局配置，通常只做 console 或空操作 |
| `mount` | 每次进入子应用路由时调用 | 创建应用实例并渲染到 `container` 内的 DOM 节点 |
| `unmount` | 每次离开子应用路由时调用 | 销毁应用实例，清理 DOM 和事件，释放内存 |

---

## 六、子应用 D（sub-app-d, Vue 3 + Webpack）的微前端配置

与 sub-app-c 的接入方式完全一致，只是框架从 React 换成 Vue 3，实例管理方式和路由处理方式不同。

### 6.1 public-path.js

与 sub-app-c 完全相同。

### 6.2 Webpack 配置

```js
module.exports = {
  output: {
    // 导出为 umd 格式，供主应用 qiankun 获取子应用导出的生命周期
    library: 'sub-app-d',
    libraryTarget: 'umd',
    // 避免与其它子应用的 chunk 加载全局变量冲突
    chunkLoadingGlobal: 'webpackJsonp_sub_app_d'
  },
  devServer: {
    // 使用自定义域名，便于跨子应用 cookie / 域名隔离
    host: 'localhost.uat.com',
    port: 8083,
    // 允许主应用跨域拉取子应用资源
    headers: {
      'Access-Control-Allow-Origin': '*'
    },
    historyApiFallback: true,
    hot: true
  }
};
```

与 sub-app-c 的差异仅为 `library` 和 `chunkLoadingGlobal` 名称不同，确保多子应用之间全局变量不冲突。

### 6.3 入口文件生命周期（main.js）

```js
// 必须在最前面引入，用于动态设置 publicPath
import './public-path';
import { createApp } from 'vue';
import App from './App.vue';
import createAppRouter from './router/index';

// 子应用根实例引用
let app = null;

/**
 * 渲染子应用到挂载节点
 * @param {Object} props qiankun 注入的参数对象（可含 container 等）
 */
function render(props) {
  // 解构出挂载容器
  const { container } = props || {};
  // 被主应用加载时用 container 内的 #app，独立运行时用 document 的 #app
  const mountNode = container ? container.querySelector('#app') : document.getElementById('app');
  // 根据运行环境设置路由 basename，qiankun 环境下用 activeRule 作为 basename
  const basename = window.__POWERED_BY_QIANKUN__ ? '/qiankun/sub-d' : '/';
  // 创建路由实例
  const router = createAppRouter(basename);
  app = createApp(App);
  app.use(router);
  app.mount(mountNode);
}

// 独立运行（非 qiankun 环境）时直接渲染
if (!window.__POWERED_BY_QIANKUN__) {
  render({});
}

/**
 * bootstrap：子应用首次激活时调用，只执行一次
 */
export async function bootstrap() {
  console.log('子应用 D bootstrap');
}

/**
 * mount：每次进入子应用时调用
 * @param {Object} props qiankun 注入的参数对象
 */
export async function mount(props) {
  render(props);
}

/**
 * unmount：每次离开子应用时调用
 * @param {Object} props qiankun 注入的参数对象
 */
export async function unmount(props) {
  if (app) {
    app.unmount();
    app = null;
  }
}
```

### 6.4 路由配置（router/index.js）

Vue 子应用的路由处理与 React 子应用有本质区别：

```js
import { createRouter, createWebHistory, createMemoryHistory } from 'vue-router';
import List from '../pages/List.vue';
import Detail from '../pages/Detail.vue';
import Edit from '../pages/Edit.vue';

// 基础路由配置表（独立运行时使用，路径不含前缀）
const baseRoutes = [
  // 列表页 - 默认首页
  { path: '/', component: List },
  // 详情页
  { path: '/detail', component: Detail },
  // 编辑页
  { path: '/edit', component: Edit }
];

/**
 * 创建路由实例
 * @param {string} basename 路由基础路径
 * @returns {Object} 路由实例
 */
function createAppRouter(basename) {
  const isQiankun = window.__POWERED_BY_QIANKUN__;

  if (isQiankun) {
    // 主应用使用 HashRouter，hash 已被主应用路由占用（如 #/qiankun/sub-d），
    // Vue 的 createWebHashHistory 无法像 React HashRouter 那样从 hash 中剥离 basename，
    // 改用 createMemoryHistory 不操作 URL，子应用内部导航在内存中进行
    return createRouter({
      history: createMemoryHistory(),
      routes: baseRoutes,
    });
  }

  // 独立运行时使用 createWebHistory，basename 由 Vue Router 自动剥离
  return createRouter({
    history: createWebHistory(basename),
    routes: baseRoutes,
  });
}

export default createAppRouter;
```

> **关键区别：Vue 子应用在 qiankun 环境下使用 `createMemoryHistory` 而非 HashRouter。**
>
> 主应用使用 HashRouter，URL 的 hash 已被主应用路由占用（如 `#/qiankun/sub-d`）。React 的 `HashRouter` 可以通过 `basename` 从 hash 中剥离前缀，但 Vue 的 `createWebHashHistory` 没有类似机制。因此 Vue 子应用改用 `createMemoryHistory`，不操作 URL，子应用内部导航完全在内存中进行，避免与主应用 hash 路由冲突。

### 6.5 与 sub-app-c 的差异对比

| 差异点 | sub-app-c (React) | sub-app-d (Vue 3) |
|--------|-------------------|-------------------|
| 挂载节点 ID | `#root` | `#app` |
| 创建实例 | `createRoot(mountNode).render(<App />)` | `createApp(App).mount(mountNode)` |
| 卸载实例 | `root.unmount()` | `app.unmount()` |
| qiankun 环境路由 | `HashRouter` + `basename` | `createMemoryHistory`（内存路由） |
| 独立运行路由 | `BrowserRouter` + `basename` | `createWebHistory(basename)` |

---

## 七、子应用 E（sub-app-e, Vue 3 + Vite）的微前端配置

与 sub-app-b 同为 Vite 子应用，使用 `vite-plugin-qiankun` 插件接入。但因 Vue 不使用 JSX，无需配置 `esbuild.jsx`，转而需要 `@vitejs/plugin-vue` 处理 `.vue` 文件。

### 7.1 安装依赖

在 `package.json` 中引入：

```json
"vite-plugin-qiankun": "^1.0.15",
"@vitejs/plugin-vue": "^5.0.0"
```

**作用：** `vite-plugin-qiankun` 用于 qiankun 生命周期适配；`@vitejs/plugin-vue` 用于编译 Vue 单文件组件（.vue）。

### 7.2 Vite 配置（vite.config.js）

```js
import { defineConfig } from 'vite';
import vue from '@vitejs/plugin-vue';
import qiankun from 'vite-plugin-qiankun';

// Vite + Vue 子应用接入 qiankun 配置
export default defineConfig({
  plugins: [
    // 处理 .vue 单文件组件
    vue(),
    // 注入子应用名称，qiankun 据此识别并接管子应用生命周期
    qiankun('sub-app-e', { useDevMode: true })
  ],
  server: {
    // 使用自定义域名，便于跨子应用 cookie / 域名隔离
    host: 'localhost.uat.com',
    port: 5175,
    // 允许主应用跨域拉取子应用资源
    cors: true,
    // 固定资源 origin，避免被主应用加载时资源路径 404
    origin: 'http://localhost.uat.com:5175'
  }
});
```

| 配置 | 作用 |
|------|------|
| `vue()` | `@vitejs/plugin-vue` 插件，处理 `.vue` 单文件组件的编译 |
| `qiankun('sub-app-e', ...)` | 第一个参数是子应用名称，必须与主应用 `microApps.js` 中的 `name` 一致。`useDevMode: true` 开启开发模式适配 |
| `server.host: 'localhost.uat.com'` | 使用自定义域名，与主应用保持一致 |
| `server.cors: true` | 允许主应用（localhost.uat.com:8080）跨域请求子应用（localhost.uat.com:5175）的资源 |
| `server.origin` | 固定静态资源的 origin 为子应用地址。Vite 默认会用相对路径，被主应用加载时会导致资源 404，必须固定为绝对地址 |

> **与 sub-app-b 的关键区别：** sub-app-b 需要配置 `esbuild.jsx: 'automatic'` 来规避 React 的 `react-refresh` 与 qiankun 沙箱冲突；sub-app-e 使用 Vue，不涉及 JSX，无需此配置，但需要 `vue()` 插件处理 `.vue` 文件。

### 7.3 入口文件生命周期导出（main.js）

```js
import { createApp } from 'vue';
import { renderWithQiankun, qiankunWindow } from 'vite-plugin-qiankun/dist/helper';
import App from './App.vue';
import createAppRouter from './router/index';

// 子应用根实例引用
let app = null;

/**
 * 渲染子应用到挂载节点
 * @param {Object} props qiankun 注入的参数对象（可含 container 等）
 */
function render(props) {
  // 解构出挂载容器
  const { container } = props || {};
  // 被主应用加载时用 container 内的 #app，独立运行时用 document 的 #app
  const mountNode = container ? container.querySelector('#app') : document.getElementById('app');
  // 根据运行环境设置路由 basename，qiankun 环境下用 activeRule 作为 basename
  const basename = qiankunWindow.__POWERED_BY_QIANKUN__ ? '/qiankun/sub-e' : '/';
  // 创建路由实例
  const router = createAppRouter(basename);
  app = createApp(App);
  app.use(router);
  app.mount(mountNode);
}

// 通过 vite-plugin-qiankun 导出生命周期，并兼容独立运行
renderWithQiankun({
  /**
   * bootstrap：子应用首次激活时调用一次
   */
  bootstrap() {
    console.log('子应用 E bootstrap');
  },
  /**
   * mount：每次进入子应用时调用
   * @param {Object} props qiankun 注入的参数对象
   */
  mount(props) {
    render(props);
  },
  /**
   * unmount：每次离开子应用时调用
   * @param {Object} props qiankun 注入的参数对象
   */
  unmount(props) {
    if (app) {
      app.unmount();
      app = null;
    }
  },
  update() {}
});

// 独立运行（非 qiankun 环境）时直接渲染
if (!qiankunWindow.__POWERED_BY_QIANKUN__) {
  render({});
}
```

| 配置 | 作用 |
|------|------|
| `renderWithQiankun()` | `vite-plugin-qiankun` 提供的辅助函数，替代手动 `export` 生命周期。内部将 `bootstrap/mount/unmount` 注册到 qiankun 能识别的全局变量上 |
| `qiankunWindow.__POWERED_BY_QIANKUN__` | Vite 子应用不能直接访问 `window.__POWERED_BY_QIANKUN__`（沙箱隔离），需通过 `qiankunWindow` 获取。用于判断是否被主应用加载 |
| `basename` | qiankun 环境下设为 `/qiankun/sub-e`，独立运行时为 `/` |
| `container` | 被主应用加载时，qiankun 传入 `container`（即 `#sub-app-viewport` 的 DOM），子应用在其内部查找 `#app` 挂载；独立运行时直接用 `document.getElementById('app')` |

### 7.4 路由配置（router/index.js）

与 sub-app-d 的路由配置方式一致，区别在于环境检测变量使用 `qiankunWindow` 而非 `window`：

```js
import { createRouter, createWebHistory, createMemoryHistory } from 'vue-router';
import { qiankunWindow } from 'vite-plugin-qiankun/dist/helper';

/**
 * 创建路由实例
 * @param {string} basename 路由基础路径
 * @returns {Object} 路由实例
 */
function createAppRouter(basename) {
  // Vite 子应用需通过 qiankunWindow 访问 qiankun 注入的变量（非 window）
  const isQiankun = qiankunWindow.__POWERED_BY_QIANKUN__;

  if (isQiankun) {
    // 主应用使用 HashRouter，hash 已被主应用路由占用（如 #/qiankun/sub-e），
    // Vue 的 createWebHashHistory 无法像 React HashRouter 那样从 hash 中剥离 basename，
    // 改用 createMemoryHistory 不操作 URL，子应用内部导航在内存中进行
    return createRouter({
      history: createMemoryHistory(),
      routes: baseRoutes,
    });
  }

  // 独立运行时使用 createWebHistory，basename 由 Vue Router 自动剥离
  return createRouter({
    history: createWebHistory(basename),
    routes: baseRoutes,
  });
}
```

### 7.5 与 sub-app-b 的差异对比

| 差异点 | sub-app-b (React + Vite) | sub-app-e (Vue 3 + Vite) |
|--------|--------------------------|--------------------------|
| 框架插件 | 无（使用 esbuild 内置 JSX 转换） | `@vitejs/plugin-vue` |
| JSX 配置 | `esbuild.jsx: 'automatic'`（规避 react-refresh 冲突） | 不需要（Vue 不使用 JSX） |
| 挂载节点 ID | `#root` | `#app` |
| 创建实例 | `createRoot(mountNode).render(<App />)` | `createApp(App).mount(mountNode)` |
| 卸载实例 | `root.unmount()` | `app.unmount()` |
| qiankun 环境路由 | `HashRouter` + `basename` | `createMemoryHistory`（内存路由） |
| 环境检测变量 | `qiankunWindow.__POWERED_BY_QIANKUN__` | `qiankunWindow.__POWERED_BY_QIANKUN__` |

> **注意：Vite 子应用均不需要 `public-path.js`**，因为 `vite-plugin-qiankun` 和 `server.origin` 配置已解决了资源路径问题。

---

## 八、B / C / D / E 四个子工程微前端配置完整对比

### 8.1 接入方式总览

| 对比维度 | sub-app-b (React+Vite) | sub-app-c (React+Webpack) | sub-app-d (Vue3+Webpack) | sub-app-e (Vue3+Vite) |
|---------|------------------------|---------------------------|--------------------------|------------------------|
| 端口 | 5174 | 8082 | 8083 | 5175 |
| 生命周期导出方式 | `renderWithQiankun()` 辅助函数 | 手动 `export` 三个函数 | 手动 `export` 三个函数 | `renderWithQiankun()` 辅助函数 |
| 打包格式 | ESM（Vite 默认） | UMD（`libraryTarget: 'umd'`） | UMD（`libraryTarget: 'umd'`） | ESM（Vite 默认） |
| 资源路径处理 | `server.origin` 固定绝对地址 | `public-path.js` 动态设置 | `public-path.js` 动态设置 | `server.origin` 固定绝对地址 |
| 环境检测变量 | `qiankunWindow.__POWERED_BY_QIANKUN__` | `window.__POWERED_BY_QIANKUN__` | `window.__POWERED_BY_QIANKUN__` | `qiankunWindow.__POWERED_BY_QIANKUN__` |
| 跨域配置 | `server.cors: true` | `devServer.headers: CORS` | `devServer.headers: CORS` | `server.cors: true` |
| host 配置 | `localhost.uat.com` | `localhost.uat.com` | `localhost.uat.com` | `localhost.uat.com` |
| 核心额外依赖 | `vite-plugin-qiankun` | 无 | 无 | `vite-plugin-qiankun` + `@vitejs/plugin-vue` |
| 需要 public-path.js | 否 | 是 | 是 | 否 |
| 需要 UMD library 配置 | 否 | 是 | 是 | 否 |
| 需要 chunkLoadingGlobal | 否 | 是 | 是 | 否 |

### 8.2 框架差异对比

| 对比维度 | sub-app-b (React) | sub-app-c (React) | sub-app-d (Vue 3) | sub-app-e (Vue 3) |
|---------|-------------------|-------------------|--------------------|--------------------|
| 挂载节点 ID | `#root` | `#root` | `#app` | `#app` |
| 创建实例 | `createRoot(mountNode).render(<App />)` | `createRoot(mountNode).render(<App />)` | `createApp(App).mount(mountNode)` | `createApp(App).mount(mountNode)` |
| 卸载实例 | `root.unmount()` | `root.unmount()` | `app.unmount()` | `app.unmount()` |
| 组件格式 | `.jsx` | `.jsx` | `.vue` SFC | `.vue` SFC |
| qiankun 环境路由 | `HashRouter` + `basename` | `HashRouter` + `basename` | `createMemoryHistory` | `createMemoryHistory` |
| 独立运行路由 | `BrowserRouter` + `basename` | `BrowserRouter` + `basename` | `createWebHistory(basename)` | `createWebHistory(basename)` |

### 8.3 Vite vs Webpack 接入差异的根本原因

| 对比维度 | Vite 子应用（B、E） | Webpack 子应用（C、D） |
|---------|--------------------|-----------------------|
| 开发模式产物 | 原生 ESM + 浏览器 `import` | UMD 格式，挂载到 `window` |
| 生命周期获取 | `vite-plugin-qiankun` 适配层注入 | qiankun 直接从 `window[library]` 读取 |
| 资源路径 | `server.origin` 固定绝对地址 | `public-path.js` 动态修改 `__webpack_public_path__` |
| 全局变量冲突风险 | 无（ESM 无全局变量） | 有（需唯一 `chunkLoadingGlobal`） |

**根本原因：** Vite 开发模式使用原生 ESM + 浏览器原生 `import`，构建产物没有全局变量挂载机制，qiankun 默认的 `import-html-entry` 无法直接获取生命周期函数，因此需要 `vite-plugin-qiankun` 做适配。而 Webpack 的 UMD 格式天然支持将导出挂到 `window` 上，qiankun 可以直接读取。

### 8.4 React vs Vue 接入差异

| 对比维度 | React 子应用（B、C） | Vue 3 子应用（D、E） |
|---------|---------------------|----------------------|
| 挂载节点 ID | `#root` | `#app` |
| 实例创建 API | `createRoot` from `react-dom/client` | `createApp` from `vue` |
| 实例渲染方式 | `.render(<App />)` | `.mount(mountNode)` |
| 实例卸载 API | `root.unmount()` | `app.unmount()` |
| 组件文件格式 | `.jsx` | `.vue`（SFC） |
| qiankun 环境路由方案 | `HashRouter` + `basename` | `createMemoryHistory`（内存路由） |
| Vite 下额外注意 | 需 `esbuild.jsx: 'automatic'` 规避 react-refresh 冲突 | 需 `@vitejs/plugin-vue` 编译 SFC，无 JSX 冲突问题 |

### 8.5 React vs Vue 路由处理的根本区别

主应用使用 HashRouter，URL hash 已被主应用路由占用（如 `#/qiankun/sub-b`）。React 和 Vue 对此的处理方式不同：

- **React 子应用**：`HashRouter` 组件支持 `basename` 属性，能从 hash 中剥离前缀（如 `basename="/qiankun/sub-b"`），因此 qiankun 环境下直接使用 `HashRouter`
- **Vue 子应用**：`createWebHashHistory` 没有类似 `basename` 的剥离机制，无法从 hash 中分离子应用路由。因此改用 `createMemoryHistory`，子应用内部导航完全不操作 URL，在内存中进行，避免与主应用 hash 路由冲突

---

## 九、wecodesite 工程的 loadMicroApp 模式

wecodesite 是另一个基于 qiankun 的主应用工程，与 qiankunProject/main-app 使用不同的 qiankun 接入模式。

### 9.1 与 qiankunProject 的模式对比

| 对比维度 | qiankunProject（registerMicroApps 模式） | wecodesite（loadMicroApp 模式） |
|---------|------------------------------------------|-------------------------------|
| 构建工具 | Webpack 5 | Vite 5 |
| qiankun 接入方式 | `registerMicroApps` + `start`（自动路由匹配） | `loadMicroApp`（主动加载/卸载） |
| 路由匹配 | qiankun 自动监听 URL 变化匹配 activeRule | 在 `useEffect` 中主动判断路由并加载 |
| 子应用卸载 | qiankun 自动卸载 | 在 `useEffect` 清理函数中主动调用 `handle.unmount()` |
| sandbox 配置 | 在 `start()` 中全局配置 | 在每次 `loadMicroApp` 调用时单独配置 |
| prefetch | `start({ prefetch: true })` 全局配置 | 无（主动模式不支持自动预加载） |
| microApps.js 额外字段 | 无 | `menuKey`（供侧边栏菜单高亮使用） |

### 9.2 loadMicroApp 的核心代码（Layout.jsx）

```jsx
import React, { useEffect } from 'react';
import { Outlet, useLocation } from 'react-router-dom';
import { loadMicroApp } from 'qiankun';
import microApps from '../../microApps';

function LayoutInner() {
  const location = useLocation();

  // 判断是否为微前端子应用页面（qiankun 子应用统一以 /qiankun/ 为前缀）
  const isMicroAppPage = location.pathname.startsWith('/qiankun/');

  // 根据当前路由匹配对应的微前端子应用名称
  const currentMicroAppName = isMicroAppPage
    ? microApps.find((app) => location.pathname.startsWith('/' + app.menuKey))?.name
    : null;

  // 主动加载/卸载微前端子应用（兼容 hash 路由模式）
  useEffect(() => {
    if (!currentMicroAppName) return;

    const matchedApp = microApps.find((app) => app.name === currentMicroAppName);
    if (!matchedApp) return;

    // loadMicroApp 返回 MicroApp 句柄对象（非 Promise），包含 mount/unmount 方法
    const handle = loadMicroApp(
      {
        name: matchedApp.name,
        entry: matchedApp.entry,
        container: '#sub-app-viewport',
      },
      {
        sandbox: { experimentalStyleIsolation: true },
      }
    );

    // 清理函数：离开子应用路由时卸载子应用
    return () => {
      handle.unmount();
    };
  }, [currentMicroAppName]);

  return (
    <AntLayout>
      <Content>
        {/* 微前端子应用页面：隐藏 Outlet，显示 qiankun 挂载容器 */}
        {isMicroAppPage ? (
          <div id="sub-app-viewport" style={{ minHeight: '100%' }} />
        ) : (
          <Outlet />
        )}
      </Content>
    </AntLayout>
  );
}
```

| 配置 | 作用 |
|------|------|
| `loadMicroApp()` | qiankun 提供的主动加载子应用 API，返回 MicroApp 句柄对象（非 Promise），包含 `mount`/`unmount` 方法 |
| `useEffect` + 依赖 `currentMicroAppName` | 当路由变化导致匹配的子应用名称变化时，自动卸载旧子应用并加载新子应用 |
| `handle.unmount()` | 在 `useEffect` 清理函数中调用，确保离开子应用路由时正确卸载子应用，避免内存泄漏 |
| `sandbox` 在 `loadMicroApp` 第二个参数中配置 | 主动模式下 sandbox 配置在每次调用时传入，而非全局 `start()` |
| `isMicroAppPage` 条件渲染 | 子应用页面时显示 `#sub-app-viewport` 容器并隐藏 `Outlet`；非子应用页面时显示 `Outlet` |

### 9.3 两种模式的适用场景

| 模式 | 优点 | 缺点 | 适用场景 |
|------|------|------|---------|
| `registerMicroApps` + `start` | 自动路由匹配，支持 prefetch 预加载，代码简洁 | 全局配置，灵活性较低 | 标准微前端场景，子应用与路由一一对应 |
| `loadMicroApp` | 主动控制加载/卸载时机，灵活度高；可在同页面加载多个子应用 | 无自动 prefetch，需主动管理生命周期；代码更复杂 | 需要精细控制子应用加载时机的场景，或同页面多子应用共存 |

---

## 十、其他微前端框架/方案对比

除了 qiankun 之外，业界还有多种成熟的微前端方案，各有不同的设计理念和适用场景。

### 10.1 主流方案总览

| 框架/方案 | 开发团队 | 技术基础 | JS 沙箱 | CSS 隔离 | Vite 支持 | 子应用保活 | 多实例 |
|-----------|---------|---------|---------|---------|-----------|-----------|--------|
| **qiankun** | 蚂蚁金服 | single-spa + HTML entry | Proxy 沙箱 | Shadow DOM / Scoped | 需插件 | 不支持 | 不支持 |
| **single-spa** | 开源社区 | 路由调度 + 生命周期 | 无 | 无 | 原生支持 | 不支持 | 支持 |
| **micro-app** | 京东 | WebComponent + qiankun 沙箱 | Proxy 沙箱 | Shadow DOM | 需插件 | 支持 | 支持 |
| **wujie（无界）** | 腾讯 | WebComponent + iframe | iframe 天然隔离 | iframe 天然隔离 | 原生支持 | 支持 | 支持 |
| **Module Federation** | Webpack 官方 | 构建时模块共享 | 无 | 无 | 有 Vite 插件 | 不支持 | 不支持 |
| **Garfish** | 字节跳动 | Proxy 沙箱 + 动态样式 | Proxy 沙箱 | 动态隔离 | 支持 | 支持 | 支持 |
| **EMP** | 欢聚集团 | Module Federation 封装 | 无 | 无 | 不支持 | 不支持 | 不支持 |
| **iframe** | 浏览器原生 | iframe 标签 | 天然隔离 | 天然隔离 | 原生支持 | 不适用 | 支持 |

### 10.2 各方案详细分析

#### 10.2.1 single-spa（微前端"元框架"）

qiankun 就是基于 single-spa 的上层封装。single-spa 只提供路由调度和生命周期管理，不提供沙箱和样式隔离。

- **与 qiankun 的区别**：qiankun 在 single-spa 基础上增加了 HTML entry、JS 沙箱、CSS 隔离、资源预加载等能力；single-spa 需要手动实现这些
- **优点**：框架无关，灵活度最高；社区活跃，有 `single-spa-react`/`single-spa-vue` 等辅助库；原生支持 Vite ESM
- **缺点**：不提供沙箱/样式隔离/通信机制，需全部手动实现；配置繁琐，学习曲线陡峭
- **适用**：需要深度自定义微前端体系的场景，或已有成熟隔离方案的项目

#### 10.2.2 micro-app（京东）

使用 WebComponent（自定义元素 `<micro-app>`）加载子应用，而非 qiankun 的 HTML entry + 路由监听。

- **与 qiankun 的区别**：组件式 API（`<micro-app src="...">`），更符合组件化思维；支持子应用保活和多实例；复用了 qiankun 的沙箱机制
- **优点**：子应用侵入性更低，接入成本更低；支持子应用保活（切走不销毁，切回不重新渲染）；支持多实例（同页面多个子应用）
- **缺点**：沙箱仍用 Proxy，有性能损耗；Vite 支持需插件且 JS 无法沙箱隔离；IE 不兼容
- **适用**：渐进式迁移存量项目，需要子应用保活的场景

#### 10.2.3 wujie / 无界（腾讯）

用 iframe 做 JS 沙箱（天然隔离），用 WebComponent 做 CSS 隔离容器。

- **与 qiankun 的区别**：隔离方式从 Proxy 沙箱改为 iframe 天然隔离，彻底解决 JS 隔离问题；CSS 隔离从 Shadow DOM/Scoped 改为 WebComponent 容器
- **优点**：隔离最彻底（iframe 天然隔离，无 Proxy 性能损耗）；原生支持 Vite ESM；支持子应用保活和多实例；预加载能力强
- **缺点**：iframe 历史包袱（弹窗挂载到 body 的问题、滚动条问题）；React 16 兼容性差；跨 iframe 通信成本较高
- **适用**：对隔离性和安全性要求极高的场景（金融、医疗）；以 Vite 为主的项目

#### 10.2.4 Module Federation（Webpack 5 / Rspack 原生功能）

基于构建时模块共享，运行时动态加载远程模块，而非运行时加载整个子应用。

- **与 qiankun 的区别**：粒度不同——Module Federation 是模块级共享（可共享单个组件/函数），qiankun 是应用级加载（加载整个子应用）；Module Federation 天然支持依赖去重（React/Vue 只加载一次）
- **优点**：模块级共享，代码复用率最高；依赖去重，性能最优（无重复加载 React/Vue）；Rspack 版本构建速度极快
- **缺点**：强依赖 Webpack 5 / Rspack；无沙箱和样式隔离（需自行实现或靠约定）；不支持子应用保活；要求技术栈统一（同版本构建工具）
- **适用**：技术栈统一的新项目，追求极致性能和模块共享的多团队协作

#### 10.2.5 Garfish（字节跳动）

类似 qiankun 的运行时方案，但在多实例和 Vite 支持方面更完善。

- **与 qiankun 的区别**：原生支持多实例和子应用保活；原生支持 Vite；资源调度更高效（并行加载、缓存优化）
- **优点**：高效资源管理，支持并行加载和缓存优化；强大的扩展性；原生 Vite 支持
- **缺点**：文档和社区活跃度不如 qiankun；对构建工具链的整合需一定学习成本
- **适用**：大型应用跨团队协作，需高效资源调度，技术栈混合且追求性能

#### 10.2.6 iframe（浏览器原生方案）

最简单直接的隔离方案，每个子应用运行在独立 iframe 中。

- **与 qiankun 的区别**：无需任何框架，浏览器原生隔离
- **优点**：完全隔离（JS/CSS/路由），安全性最高；技术栈完全无关；实现最简单
- **缺点**：性能差（每个 iframe 独立浏览器上下文，资源消耗大）；交互受限（全局弹窗、拖拽等跨应用交互困难）；路由问题（前进/后退按钮异常）；SEO 不友好；通信复杂（需 postMessage）
- **适用**：需要完全隔离的第三方应用嵌入；对性能要求不高的简单场景

### 10.3 选型建议

| 场景 | 推荐方案 | 理由 |
|------|---------|------|
| 多技术栈共存 + 存量项目迁移 | **qiankun** 或 **micro-app** | 成熟稳定，社区大，多框架支持好 |
| 对隔离性/安全性要求极高 | **wujie** | iframe 天然隔离，最安全 |
| 技术栈统一 + 追求极致性能 | **Module Federation** | 模块共享，依赖去重，性能最优 |
| 需要子应用保活 + 多实例 | **wujie** 或 **micro-app** | qiankun 不支持保活和多实例 |
| Vite 子应用为主 | **wujie** 或 **Garfish** | 原生支持 Vite ESM，无需额外插件 |
| 从零搭建新项目（统一技术栈） | **Module Federation (Rspack)** | 构建快，模块共享，架构更现代 |
| 需要完全隔离的第三方嵌入 | **iframe** | 最简单，最安全，无需框架 |
| 需要深度自定义微前端体系 | **single-spa** | 灵活度最高，可完全控制 |

### 10.4 qiankun 与 micro-app 深度对比

qiankun 和 micro-app 是业界最主流的两款运行时微前端框架，设计理念接近但实现细节差异较大。下面从子应用接入与变更、保活机制、路由限制、沙箱隔离、构建配置五个维度做深度对比。

#### 10.4.1 子应用接入与变更

**接入方式**

- **qiankun**：主应用通过 `registerMicroApps()` 全局注册，声明每个子应用的 `name` / `entry` / `container` / `activeRule`，再调用 `start()` 启动。子应用需要导出 `bootstrap` / `mount` / `unmount` 生命周期函数、新建 `public-path.js`、配置 UMD 打包格式。接入成本较高，子应用有侵入式改造。
- **micro-app**：主应用以组件方式使用 `<micro-app src="...">` 标签直接引入子应用，无需全局注册和路由匹配。子应用基本无需改造，不用导出生命周期函数，接入成本极低。

**src / entry 传入的内容**

两者传入的都是子应用的 **HTML entry 地址（页面访问 URL）**，例如 `http://localhost:3001`，而不是打包后的某个资源文件路径（如 `main.js`）。框架会 fetch 该 URL 获取子应用的 HTML 页面，解析其中的 `<script>` / `<link>` 资源并在沙箱中执行。所以 micro-app 的 `src` ≈ qiankun 的 `entry`，都是子应用入口页地址。

**子应用多页面时 src 的处理**

`src` 始终指向子应用的**入口地址（base URL）**，不会因为子应用有多个页面而变化。子应用内部的多页面路由由子应用自身的路由系统（React Router / Vue Router）管理，micro-app 只负责加载子应用入口，不关心子应用内部有多少页面。如果需要从主应用直接打开子应用的某个具体页面，依靠的是路由能力（见 10.4.3 节），而非修改 `src`。

**变更与实时更新**

- **子应用代码变更**：两者都通过 HTML entry 动态拉取资源，子应用重新部署后，主应用重新加载时获取的是最新版本（实时更新），无需主应用做任何改动。
- **子应用接入配置变更**（如新增/删除子应用、修改入口地址）：
  - qiankun 需要修改主应用的 `microApps.js` 配置并重新部署主应用。
  - micro-app 由于是组件式声明，同样需要修改主应用代码中 `<micro-app>` 标签的 `src` 或增删标签，然后重新部署主应用。

| 对比维度 | qiankun | micro-app |
|---------|---------|-----------|
| 接入 API | `registerMicroApps()` + `start()` 全局注册 | `<micro-app src="...">` 组件式声明 |
| 子应用改造 | 导出生命周期、public-path、UMD 打包 | 基本无需改造 |
| src / entry 含义 | `entry` 为子应用 HTML 页面 URL | `src` 为子应用 HTML 页面 URL |
| 多页面子应用 src | 入口 base URL，不变 | 入口 base URL，不变 |
| 子应用代码变更 | 实时更新（重新加载拉取最新资源） | 实时更新（重新加载拉取最新资源） |
| 接入配置变更 | 改主应用注册配置 + 重新部署 | 改主应用 `<micro-app>` 标签 + 重新部署 |
| 接入成本 | 较高（子应用侵入式改造） | 极低（子应用几乎无感） |

#### 10.4.2 保活机制（切换是否刷新）

**qiankun：不支持保活，切换即刷新**

qiankun 切换子应用时会执行 `unmount` 卸载子应用（销毁 DOM 和实例），切回时重新执行 `mount` 重新渲染整个子应用。因此每次切换都有"刷新感"，子应用的内部状态（如表单输入、滚动位置、组件状态）会丢失。

**micro-app：支持保活，切换不刷新**

micro-app 通过 `keep-alive` 属性支持子应用保活。设置 `<micro-app src="..." keep-alive>` 后，切走子应用时不会销毁其 DOM 和状态，而是隐藏保留；切回时直接恢复显示，不重新渲染。子应用的内部状态、滚动位置、组件实例全部保留，无"刷新感"。

**micro-app 切换子应用的两种方式：**

需要注意的是，micro-app 切换子应用并非只改 `src` 属性那么简单，`keep-alive` 的保活效果取决于切换方式：

- **方式1：单个标签改 src（无保活）**
  - 只用一个 `<micro-app>` 标签，切换时修改其 `src` 属性。
  - micro-app 会卸载当前子应用、加载新的子应用，相当于重新渲染，状态丢失。
  - 效果与 qiankun 的 unmount/mount 切换类似，没有发挥保活优势。
  - 即使加了 `keep-alive`，由于只有一个标签承载多个子应用，切换 src 仍会触发重新加载。

- **方式2：多个标签 + 显示/隐藏（保活，推荐）**
  - 为每个子应用各放置一个独立的 `<micro-app>` 标签，通过控制标签的显示/隐藏来切换。
  - 显示/隐藏必须用 `v-show`（Vue）或 `display:none`（原生）等样式控制方式，**不能用 `v-if` 或条件渲染**销毁标签，否则保活失效。
  - 配合 `keep-alive` 属性：切走时隐藏对应标签（DOM 和状态保留不销毁），切回时恢复显示（不重新渲染，状态保留）。
  - 这才是 micro-app 保活机制的正确实现方式。

| 对比维度 | qiankun | micro-app |
|---------|---------|-----------|
| 是否支持保活 | 不支持 | 支持（`keep-alive` 属性） |
| 切走行为 | `unmount` 卸载，销毁 DOM 和实例 | 隐藏保留，DOM 和状态不销毁 |
| 切回行为 | 重新 `mount` 渲染 | 恢复显示，不重新渲染 |
| 内部状态保留 | 不保留（丢失） | 保留 |
| 刷新感 | 有（重新渲染） | 无（直接恢复） |
| 性能开销 | 切回需重新执行 mount 生命周期 | 切回无生命周期开销 |
| 切换实现方式 | 路由匹配自动卸载/加载 | 多标签 + 显示/隐藏（保活）；单标签改 src（无保活） |
| 保活前提条件 | 不适用 | 多标签 + `v-show`/`display:none` + `keep-alive`，禁用 `v-if` |

**保活机制对子应用版本更新的影响**

保活机制在保留状态的同时，也带来了子应用新版本不能即时生效的问题：

- **micro-app（keep-alive）**：保活中的子应用切走时 DOM 和 JS 实例都保留在内存中，切回时直接恢复显示，**不会重新 fetch HTML entry**。因此子应用部署新版本后，保活中的子应用仍运行旧版本代码，只有**销毁保活实例后重新加载**（移除标签、取消 keep-alive、调用 unmount）或**刷新整个页面**，micro-app 才会重新 fetch HTML entry 获取最新版本资源。
- **qiankun**：不支持保活，每次切换都执行 `unmount`（销毁）-> `mount`（重新渲染）。每次 mount 时都会重新 fetch HTML entry，所以切走再切回理论上能获取到最新版本（实际是否拿到新资源还取决于子应用 HTTP 缓存策略：`index.html` 不缓存 + JS/CSS 带 hash 长缓存是标准做法）。

| 场景 | qiankun | micro-app（keep-alive） |
|------|---------|------------------------|
| 子应用部署新版本，用户切走再切回 | 重新 mount，fetch 新版本（受 HTTP 缓存影响） | 保留旧实例，运行旧版本 |
| 用户刷新整个页面（F5） | 所有子应用重新加载，获取最新版本 | 所有子应用重新加载，获取最新版本 |
| 用户手动关闭子应用标签后重新打开 | 重新加载，获取最新版本 | 重新加载，获取最新版本 |
| 子应用长时间保活运行 | 不适用（每次切换都重新加载） | 一直运行旧版本，直到销毁或刷新 |

**结论**：micro-app 保活机制会导致子应用新版本不能即时生效，需要销毁保活实例或刷新页面才能加载最新内容。这是"保活"与"实时更新"之间的固有权衡--qiankun 通过不支持保活避免了这个问题，但代价是每次切换都要重新渲染、状态丢失。

#### 10.4.3 路由限制

**qiankun：依赖 activeRule 路由匹配，主子应用需协调路由**

qiankun 通过 `activeRule` 监听浏览器 URL 变化来决定加载/卸载哪个子应用。这带来一系列路由协调问题：主应用用 HashRouter 时，子应用需用 HashRouter + basename 或 MemoryRouter；子应用用 BrowserRouter 时存在 pathname 读取冲突（本文档 12.2、12.3 节详细讨论了这些问题）。路由限制较多，主子应用路由类型需谨慎搭配。

**micro-app：基于 WebComponent，路由相对独立，支持路由同步**

micro-app 基于 WebComponent 自定义元素，子应用挂载在 `<micro-app>` 标签内，路由相对独立，不强制依赖 `activeRule` 路由匹配。micro-app 提供**路由同步**能力：

- 子应用内部使用 history API 导航时，micro-app 会拦截并同步到主应用浏览器 URL，地址栏会体现子应用路径。
- 主应用路由变化时，对应的 `<micro-app>` 组件能响应并更新子应用页面。
- 因此**主应用可以通过路由跳转到子应用的具体页面**，而不像 qiankun 那样需要 `activeRule` + basename / MemoryRouter 等复杂协调。

| 对比维度 | qiankun | micro-app |
|---------|---------|-----------|
| 路由匹配机制 | `activeRule` 监听 URL | WebComponent 组件作用域 + 路由同步 |
| 主子应用路由协调 | 需谨慎搭配（HashRouter/basename/MemoryRouter） | 相对独立，协调成本低 |
| 主应用路由跳转子应用页面 | 需 `activeRule` 匹配 + 子应用路由适配 | 支持路由同步，可直接跳转 |
| 子应用内部导航是否反映到地址栏 | 取决于路由方案（HashRouter 可以，MemoryRouter 不行） | 支持（路由同步到主应用 URL） |
| 路由限制 | 较多（见 12.2、12.3 节） | 较少 |

**前提场景：主应用使用 HashRouter 下子应用路由处理对比**

主应用使用 HashRouter 时，URL 形如 `http://host:8080/#/qiankun/sub-b`，`location.pathname` 始终为 `/`，路由信息存在于 hash 中。下面分别说明 qiankun 和 micro-app 对子应用使用 HashRouter 和 BrowserRouter 的处理方式，以及主应用如何通过修改访问路径加载子应用对应页面。

**qiankun 的处理方式：**

- **子应用使用 HashRouter**：子应用通过 `basename` 从 hash 中剥离主应用前缀，从剩余 hash 中读取自己的路由。主应用修改 URL hash（如 `#/qiankun/sub-b/detail`），`activeRule` 匹配后加载子应用，子应用通过 basename 读取到 `/detail` 并渲染对应页面。URL 反映子应用路由，刷新可保持，支持直接 URL 访问。
- **子应用使用 BrowserRouter**：BrowserRouter 从 `location.pathname` 读取路由，而 HashRouter 下 pathname 恒为 `/`，子应用无法匹配内部路由。需改用 MemoryRouter（内存中路由，不反映到 URL，刷新丢失）或自定义 history 适配层（从 hash 解析 pathname，详见 12.3 节）。主应用修改访问路径加载子应用对应页面需通过自定义 history 适配层从 hash 中解析子应用路径，实现较复杂。

**micro-app 的处理方式：**

micro-app 有两种路由模式：**虚拟路由系统（默认，推荐）** 和 **native 模式**。

- **虚拟路由系统（默认）**：通过拦截浏览器路由事件及自定义 `location`、`history`，实现一套虚拟路由系统，子应用运行在虚拟路由中，与主应用路由隔离。1.x 版本中**无路由类型约束**：主应用用 Hash，子应用可以用 Hash 或 History，都能正常工作。通过 `<micro-app baseroute="/child">` 属性下发基础路由，子应用通过 `window.__MICRO_APP_BASE_ROUTE__` 获取并设置。主应用通过 `microApp.router.push()` 控制子应用跳转到对应页面，子应用内部导航通过虚拟路由系统同步，地址栏体现子应用路径。
- **native 模式（`disable-memory-router`）**：子应用基于浏览器路由系统渲染，**有路由类型约束**：主应用是 hash 路由时，子应用也必须是 hash 路由；主应用是 history 路由时，子应用可以是 hash 或 history。子应用根据浏览器地址渲染对应页面。

**主应用通过修改访问路径加载子应用对应页面：**

- **qiankun**：主应用修改 URL hash 路径，`activeRule` 匹配后加载对应子应用。子应用用 HashRouter 时可直接读取到对应页面；子应用用 BrowserRouter 时需依赖自定义 history 适配层解析 hash，实现复杂。
- **micro-app（虚拟路由）**：主应用通过 `microApp.router.push({ name: '子应用名', path: '/detail' })` 跳转到子应用具体页面，虚拟路由系统处理路由同步，无需关心主子应用路由类型搭配，实现简单。
- **micro-app（native 模式）**：主应用修改 URL，子应用根据浏览器地址渲染对应页面，但受路由类型约束（主应用 hash 则子应用必须 hash）。

| 对比维度 | qiankun | micro-app（虚拟路由，默认） | micro-app（native 模式） |
|---------|---------|---------------------------|------------------------|
| 子应用用 HashRouter | 支持（basename 剥离前缀） | 支持（无路由类型约束） | 支持（主应用 hash 则子应用必须 hash） |
| 子应用用 BrowserRouter | 需 MemoryRouter 或自定义 history 适配层 | 支持（无路由类型约束） | 不支持（主应用 hash 时子应用必须 hash） |
| 路由类型约束 | 有（需协调主子应用路由类型） | 无（1.x 版本） | 有（主 hash 则子必须 hash） |
| 主应用跳转子应用页面 | 改 URL hash + activeRule 匹配 + 子应用适配 | `microApp.router.push()` 直接跳转 | 改 URL，子应用根据地址渲染 |
| 路由隔离 | 无（共享浏览器路由） | 有（虚拟路由系统隔离） | 无（共享浏览器路由） |
| 实现复杂度 | 较高（尤其 BrowserRouter 子应用） | 低 | 中（受路由类型约束） |

**场景示例：子应用内部路由同步到浏览器地址栏**

场景：主应用 HashRouter，访问 `http://localhost:8080/#/otherApp/sub-a` 加载子应用 a 首页，子应用 a 内部从首页跳转到详情页（路由 `/detail`），期望浏览器地址变为 `http://localhost:8080/#/otherApp/sub-a/detail`。

**qiankun 的实现（天然支持）**

子应用 a 使用 HashRouter + basename（basename 设为 `/otherApp/sub-a`）。子应用内部执行跳转（如 `history.push('/detail')`）时，HashRouter 会自动把 `basename + '/detail'` 写入 `window.location.hash`：

```
hash = '#/otherApp/sub-a' + '/detail' = '#/otherApp/sub-a/detail'
```

浏览器地址自动变为 `http://localhost:8080/#/otherApp/sub-a/detail`，无需额外处理，正是期望格式。

**micro-app 的实现（取决于路由模式）**

micro-app 有 5 种路由模式，能否实现该格式取决于模式选择：

- **search 模式（默认）**：子应用路由信息作为 **query 参数**同步到浏览器地址，格式形如 `http://localhost:8080/#/otherApp/sub-a?sub-a=/detail`（子应用名作为 query key，路由路径作为 value），**不是** `#/otherApp/sub-a/detail` 的直接拼接格式。默认模式做不到该格式。
- **native 模式**：子应用基于浏览器路由系统渲染，通过 `baseroute='/otherApp/sub-a'` 下发基础路由，子应用用 HashRouter。子应用内部跳转 `/detail` 时，HashRouter 把 `baseroute + '/detail'` 写入 hash，得到 `#/otherApp/sub-a/detail`，与 qiankun 方式一致。但 native 模式有路由类型约束：主应用 hash 则子应用必须 hash。
- **pure / state 模式**：不修改浏览器地址，无法实现该格式。

| 对比维度 | qiankun | micro-app（search 默认） | micro-app（native） |
|---------|---------|------------------------|-------------------|
| 能否得到 `#/otherApp/sub-a/detail` 格式 | 能（HashRouter + basename 自动拼接） | 不能（query 参数形式 `?sub-a=/detail`） | 能（HashRouter + baseroute 拼接） |
| 子应用内部跳转后地址栏 | 自动反映子应用路径 | 反映为 query 参数 | 自动反映子应用路径 |
| 实现方式 | basename 配置 | 默认即 query 同步，无需配置 | 需切换 native 模式 + baseroute |
| 路由类型约束 | 需协调（HashRouter+basename） | 无约束 | 有（主 hash 则子必须 hash） |

**结论**：要实现 `#/otherApp/sub-a/detail` 这种路径直接拼接的格式，qiankun 用 HashRouter + basename 即可天然实现；micro-app 需要从默认的 search 模式切换到 native 模式并配置 baseroute，默认模式做不到这个格式。

**场景补充：子应用不用 hash 路由（BrowserRouter）时 micro-app 的处理**

延续上述场景，主应用 HashRouter，访问 `http://localhost:8080/#/otherApp/sub-a` 加载子应用 a 首页，但子应用 a 使用 **BrowserRouter**（history 模式），内部从首页跳转到详情页（路由 `/detail`）。

- **micro-app 虚拟路由系统（search 模式，默认）**：虚拟路由系统拦截子应用的 `history.pushState`/`replaceState`，将子应用路由信息作为 query 参数同步到浏览器地址。**与子应用路由类型无关**：无论子应用用 HashRouter 还是 BrowserRouter，虚拟路由都拦截其导航，统一以 query 参数形式同步。子应用用 BrowserRouter 跳转 `/detail` 时，虚拟路由将 `/detail` 作为 query 参数（key 为子应用 name）追加到主应用地址：

```
http://localhost:8080/#/otherApp/sub-a?sub-a=/detail
```

注意：这与子应用用 HashRouter 时的 query 格式一致，都是 query 参数形式，不是 `#/otherApp/sub-a/detail` 的路径拼接格式。由于主应用是 HashRouter，query 参数挂在 hash 后面。

- **micro-app native 模式**：有路由类型约束，**主应用 hash 则子应用必须 hash**。子应用用 BrowserRouter（history 模式）在主应用 HashRouter 下**不支持**，无法正常工作。

| 对比维度 | qiankun | micro-app（search 默认） | micro-app（native） |
|---------|---------|------------------------|-------------------|
| 子应用用 BrowserRouter 跳转 `/detail` 后地址栏 | 需自定义 history 适配层从 hash 解析，复杂 | `#/otherApp/sub-a?sub-a=/detail`（query 参数） | 不支持（主 hash 禁止子 history） |
| 能否得到 `#/otherApp/sub-a/detail` 格式 | 需自定义 history 适配层，复杂 | 不能（query 参数形式） | 不支持 |
| 实现复杂度 | 高（需自定义 history 适配层） | 低（开箱即用，但格式为 query 参数） | 不适用 |

**结论**：micro-app 虚拟路由系统（search 模式）对 BrowserRouter 子应用的处理与 HashRouter 子应用一致，都是把子应用路由作为 query 参数同步到地址栏，格式为 `?sub-a=/detail`。native 模式则直接不支持该组合。相比 qiankun 需要自定义 history 适配层才能处理 BrowserRouter 子应用，micro-app 虚拟路由系统开箱即用但格式不同。

#### 10.4.4 沙箱隔离对比

**qiankun：Proxy 沙箱 + Shadow DOM / Scoped CSS**

- **JS 沙箱（Proxy 沙箱）**：为每个子应用创建一个 `fakeWindow`（代理对象），用 Proxy 拦截子应用对 `window` 的读写。
  - 写入：子应用 `window.xxx = val` 时，实际写到 `fakeWindow` 上，不污染真 window。
  - 读取：优先从 `fakeWindow` 取，没有再从真 window 取。
  - 不支持 Proxy 时降级为快照沙箱（mount 前快照 window，unmount 时恢复）。
- **CSS 隔离**：Shadow DOM（子应用挂到 ShadowRoot，样式天然隔离）或 Scoped CSS（运行时给选择器加属性前缀）。

**micro-app：Proxy 沙箱 + WebComponent 元素隔离 + Shadow DOM**

- **JS 沙箱（Proxy 沙箱）**：同样用 Proxy 拦截 window，机制与 qiankun 类似（复用了 qiankun 的沙箱思路）。
- **元素隔离（额外一层）**：基于 WebComponent，重写 `document.createElement` / `document.querySelector` 等方法，让子应用创建/查询的元素限定在 `<micro-app>` 自定义元素作用域内，DOM 天然有作用域边界，比 qiankun 多一层元素级隔离。
- **CSS 隔离**：Shadow DOM，子应用挂载在 ShadowRoot 内，样式天然隔离。

| 对比维度 | qiankun | micro-app |
|---------|---------|-----------|
| JS 沙箱实现 | Proxy 拦截 window（fakeWindow） | Proxy 拦截 window（同 qiankun 思路） |
| 不支持 Proxy 时 | 降级为快照沙箱 | 降级为快照沙箱 |
| 元素隔离 | 无（子应用操作全局 document） | 有（重写 document API，限定作用域） |
| CSS 隔离 | Shadow DOM / Scoped CSS | Shadow DOM |
| 隔离层级 | JS + CSS 两层 | JS + 元素 + CSS 三层 |
| Proxy 性能损耗 | 有 | 有 |

#### 10.4.5 构建配置对比

**qiankun：子应用需较多构建改造**

- **Webpack 子应用**：`output` 配置 `library` + `libraryTarget: 'umd'` + 唯一的 `chunkLoadingGlobal`；新建 `public-path.js` 并在入口最前面引入；入口文件导出生命周期函数；devServer 配置 `host` + CORS headers。
- **Vite 子应用**：需安装 `vite-plugin-qiankun` 插件；Vite 配置中引入 `qiankun()` 插件并传入子应用名称；`server` 配置 `host` + `cors` + `origin`；React 子应用需配置 `esbuild.jsx: 'automatic'`（避免 react-refresh 与 qiankun 沙箱冲突）。

**micro-app：子应用几乎无需构建改动**

- 子应用无需配置 UMD、public-path、生命周期导出，构建配置基本保持原样。
- Vite 子应用原生支持较好，但若需 JS 沙箱隔离，仍需插件配合（默认情况下 Vite ESM 模式下 JS 无法被 Proxy 沙箱完全隔离）。

| 对比维度 | qiankun | micro-app |
|---------|---------|-----------|
| Webpack 子应用改造 | UMD + public-path + 生命周期 + CORS | 基本无需改造 |
| Vite 子应用改造 | 需 vite-plugin-qiankun 插件 + 多项配置 | 基本无需改造（但 JS 沙箱需插件配合） |
| 生命周期导出 | 必须（bootstrap/mount/unmount） | 不需要 |
| public-path 配置 | 必须 | 不需要 |
| UMD 打包格式 | 必须 | 不需要 |
| Vite 下 JS 沙箱 | 插件支持 | 默认无法完全隔离，需插件配合 |
| 构建改造成本 | 较高 | 极低 |

#### 10.4.6 主子应用通信对比

**qiankun：通过 props 传递（API 式）**

当前项目使用 `loadMicroApp` 主动加载模式。`loadMicroApp` 的第二个参数中通过 `props` 字段向子应用传递数据，子应用在 `mount(props)` 生命周期函数中接收。子应用向主应用通信需主应用在 props 中传入回调函数，子应用调用该回调发送数据。

主应用发送：

```js
// 主应用 - loadMicroApp 时通过第二个参数的 props 传递数据
const handle = loadMicroApp(
  {
    name: 'sub-a',
    entry: '//localhost:3001',
    container: '#sub-app-viewport',
  },
  {
    sandbox: { experimentalStyleIsolation: true },
    // 通过 props 传递给子应用的数据
    props: {
      userId: 1001,
      token: 'abc123',
      // 传入回调函数，供子应用向主应用发送数据
      onSubAppEvent: (data) => console.log('收到子应用数据:', data)
    }
  }
)
```

子应用接收：

```js
// 子应用 - 在 mount 生命周期中接收 props
export async function mount(props) {
  // 解构出主应用下发的数据
  const { userId, token, onSubAppEvent } = props
  console.log(userId, token)

  // 子应用向主应用发送数据（调用主应用传入的回调）
  onSubAppEvent({ type: 'loaded', msg: '子应用已加载' })
}
```

**micro-app：通过 data 属性传递（组件式，发布订阅 + CustomEvent）**

micro-app 在 `<micro-app>` 标签上通过 `data` 属性传递数据，子应用通过 `window.microApp.getData()` 接收。数据通信基于发布订阅 + CustomEvent 实现，支持双向通信和数据监听。

主应用发送：

```jsx
// React 主应用 - 通过 data 属性传递
<micro-app
  name="sub-a"
  url="http://localhost:3001"
  data={{ userId: 1001, token: 'abc123' }}
/>
```

```vue
<!-- Vue 主应用 - 通过 :data 属性传递 -->
<template>
  <micro-app
    name="sub-a"
    url="http://localhost:3001"
    :data="microAppData"
    @datachange="handleDataChange"
  />
</template>
<script>
export default {
  data() {
    return {
      // 传给子应用的数据对象
      microAppData: { userId: 1001, token: 'abc123' }
    }
  },
  methods: {
    // 监听子应用向主应用发送的数据
    handleDataChange(e) {
      console.log('收到子应用数据:', e.detail.data)
    }
  }
}
</script>
```

子应用接收与发送：

```js
// 子应用 - 获取主应用下发的数据
const data = window.microApp.getData()
console.log(data) // { userId: 1001, token: 'abc123' }

// 子应用 - 监听主应用数据变化
window.microApp.addDataListener((newData) => {
  console.log('收到主应用新数据:', newData)
})

// 子应用 - 向主应用发送数据
window.microApp.dispatch({ type: 'loaded', msg: '子应用已加载' })
```

**通信机制对比**

| 对比维度 | qiankun | micro-app |
|---------|---------|-----------|
| 主应用传参方式 | `loadMicroApp` 第二参数的 `props` 字段 | `<micro-app data={...}>` 属性 |
| 子应用接收方式 | `mount(props)` 生命周期函数参数 | `window.microApp.getData()` |
| 子应用向主应用通信 | 主应用在 props 中传入回调，子应用调用 | `window.microApp.dispatch(data)` |
| 主应用监听子应用数据 | 在传入的回调函数中接收 | `@datachange` 事件（Vue）/ `addEventListener` |
| 子应用监听主应用变化 | 需手动对比 props 变化 | `window.microApp.addDataListener()` |
| 通信实现原理 | 函数传参 + 回调 | 发布订阅 + CustomEvent |
| 通信风格 | API 式（生命周期配合） | 组件式（类似 React/Vue props） |
| 是否支持响应式 | 否（需手动处理） | 是（data 变化自动通知子应用） |

#### 10.4.7 六维度总览

| 对比维度 | qiankun | micro-app |
|---------|---------|-----------|
| 子应用接入 | 全局注册 + 子应用侵入式改造 | 组件式声明 + 子应用几乎无感 |
| 接入配置变更 | 改主应用注册配置 + 重新部署 | 改主应用 `<micro-app>` 标签 + 重新部署 |
| 子应用代码变更 | 实时更新 | 实时更新 |
| 保活机制 | 不支持，切换即刷新 | 支持 `keep-alive`，切换不刷新 |
| 路由限制 | 较多（activeRule + 路由协调） | 较少（WebComponent + 路由同步） |
| 主应用路由跳子应用页面 | 需 activeRule 匹配 + 子应用适配 | 支持（路由同步） |
| JS 沙箱 | Proxy 沙箱 | Proxy 沙箱 |
| 元素隔离 | 无 | 有（WebComponent 作用域） |
| CSS 隔离 | Shadow DOM / Scoped CSS | Shadow DOM |
| 构建改造 | 较高（UMD + public-path + 生命周期） | 极低 |
| 主子应用通信 | props 传递（API 式） | data 属性 + 发布订阅（组件式） |

---

## 十一、总结：从普通工程到微前端需要添加的配置

### 11.1 主应用需要添加

1. 安装 `qiankun` 依赖
2. 创建子应用配置文件（`microApps.js`），声明每个子应用的 name / entry / container / activeRule
   - 若主应用使用 HashRouter，`activeRule` 必须用函数形式从 `location.hash` 中匹配
3. 调用 `registerMicroApps()` + `start()` 注册并启动（或使用 `loadMicroApp` 主动模式）
4. 在布局中提供子应用挂载容器 DOM 节点（`#sub-app-viewport`）
5. 添加路由占位符 `<Route path="qiankun/*" element={null} />`
6. 所有工程统一配置 `host: 'localhost.uat.com'`（需在系统 hosts 文件中添加映射）

### 11.2 Webpack 子应用需要添加（适用 C、D）

1. 新建 `public-path.js` 并在入口最前面引入
2. Webpack `output` 配置 `library` + `libraryTarget: 'umd'` + 唯一的 `chunkLoadingGlobal`
3. Webpack devServer 配置 `host` + CORS headers
4. 入口文件导出 `bootstrap` / `mount` / `unmount` 三个生命周期函数
5. 入口文件添加 `window.__POWERED_BY_QIANKUN__` 独立运行检测
6. 入口文件添加路由切换逻辑：qiankun 环境用 HashRouter（React）或 createMemoryHistory（Vue），独立运行用 BrowserRouter / createWebHistory

### 11.3 Vite + React 子应用需要添加（适用 B）

1. 安装 `vite-plugin-qiankun` 依赖
2. Vite 配置中引入 `qiankun()` 插件，传入子应用名称
3. Vite `server` 配置 `host` + `cors: true` + `origin` 固定地址
4. Vite `esbuild` 配置 `jsx: 'automatic'`（避免 react-refresh 与 qiankun 沙箱冲突）
5. 入口文件使用 `renderWithQiankun()` 注册生命周期 + `qiankunWindow.__POWERED_BY_QIANKUN__` 独立运行检测
6. 入口文件添加路由切换逻辑：qiankun 环境用 HashRouter + basename，独立运行用 BrowserRouter

### 11.4 Vite + Vue 子应用需要添加（适用 E）

1. 安装 `vite-plugin-qiankun` + `@vitejs/plugin-vue` 依赖
2. Vite 配置中引入 `vue()` 和 `qiankun()` 插件，传入子应用名称
3. Vite `server` 配置 `host` + `cors: true` + `origin` 固定地址
4. 不需要 `esbuild.jsx` 配置（Vue 不使用 JSX，无 react-refresh 冲突）
5. 入口文件使用 `renderWithQiankun()` 注册生命周期 + `qiankunWindow.__POWERED_BY_QIANKUN__` 独立运行检测
6. 路由配置中使用 `createMemoryHistory`（qiankun 环境）或 `createWebHistory`（独立运行），Vue 无法像 React 那样用 HashRouter + basename

---

## 十二、问题分析

### 12.1 子应用不用 hash 路由时的路由重复问题

**问题背景：** 如果子应用（如 sub-app-b 和 sub-app-c）不是 hash 路由，之前会出现主工程要在 `#` 前多添加一个对应路径才能正常访问。分析如果子应用不用 hash 路由，主应用应该如何修改才能解决路由重复问题。

当前主应用使用 HashRouter，子应用在 qiankun 环境下也使用 HashRouter + basename。如果子应用改用 BrowserRouter，子应用会从 `location.pathname` 读取路由，但主应用 HashRouter 下 `location.pathname` 始终为 `/`，导致子应用无法匹配内部路由。

之前出现的"主工程要在 `#` 前多添加路径"就是因为需要把子应用路径放到 pathname 中，形成类似 `http://localhost.uat.com:8080/qiankun/sub-b#/qiankun/sub-b` 的 URL 结构：pathname 部分给子应用 BrowserRouter 使用，hash 部分给主应用 HashRouter 使用，导致路径重复且维护困难。

**如果子应用不用 hash 路由，主应用的修改方案：**

主应用改用 BrowserRouter，`activeRule` 从函数匹配 hash 改为字符串匹配 pathname。URL 变为 `http://localhost.uat.com:8080/qiankun/sub-b`，不再需要 `#`，路径不再重复。但需要服务器配置 `historyApiFallback` 以支持前端路由的刷新访问。

---

### 12.2 主应用固定 HashRouter，子应用混合 Router 类型时的加载方案

**问题背景：** 如果有多个子应用，一些用 HashRouter，一些用 BrowserRouter，且主应用就需要使用 HashRouter 的情况下，有什么方法能让主应用同时都能加载对应的子应用。

主应用固定使用 HashRouter，URL 形如 `http://host:8080/#/qiankun/sub-b`，`location.pathname` 始终为 `/`。

#### HashRouter 子应用

通过 `basename` 从 hash 中剥离前缀，当前项目中 sub-app-b/c 已采用此方式，无需额外处理。

#### BrowserRouter 子应用

BrowserRouter 从 `location.pathname` 读取路由，而 HashRouter 下 pathname 恒为 `/`，子应用无法匹配内部路由。解决方案有以下几种：

**方案1：子应用在 qiankun 环境下切换为 MemoryRouter（推荐，简单）**

子应用检测 `__POWERED_BY_QIANKUN__`，为 true 时使用 MemoryRouter 替代 BrowserRouter，内部导航在内存中进行，不依赖 URL。这与 Vue 子应用使用 `createMemoryHistory` 的思路一致。缺点是子应用内部路由变化不会反映在浏览器地址栏，且存在路由参数获取问题（详见 12.3 节）。

**方案2：自定义 history 适配层（推荐，功能完整）**

使用 React Router 的基础 `<Router>` 组件，传入一个自定义 history 对象。该 history 对象从 `location.hash` 中解析出 pathname 部分（如从 `#/qiankun/sub-b/detail` 中提取 `/detail`），使 BrowserRouter 的路由逻辑能正常工作。此方案保留了 URL 驱动导航，实现详见 12.3 节。

**方案3：通过 qiankun props 传递自定义路由信息**

主应用在 `registerMicroApps` 时通过 props 传递当前路由路径，子应用 mount 时据此初始化 BrowserRouter 的 basename 和初始路径。但 BrowserRouter 仍依赖 `popstate` 事件，HashRouter 下 `popstate` 不会携带 hash 路由变化，需要额外监听 `hashchange` 事件做转换，实现较复杂。

**方案对比：**

| 方案 | 实现复杂度 | URL 反映路由 | 页面刷新保持 | 直接 URL 访问 |
|------|-----------|-------------|-------------|--------------|
| MemoryRouter | 低 | 否 | 否 | 否 |
| 自定义 history 适配层 | 中 | 是 | 是 | 是 |
| qiankun props 传递 | 高 | 否 | 需额外处理 | 需额外处理 |

---

**字段说明：**

- **URL 反映路由**：子应用内部的路由路径是否体现在浏览器地址栏中。为"是"时，用户可通过地址栏看到当前所在的子应用页面；为"否"时，地址栏仅显示主应用路由，子应用内部导航不会改变 URL。
- **页面刷新保持**：用户在子应用内部导航到某个页面后，刷新浏览器能否保持在当前页面。为"是"时，刷新后仍渲染当前路由对应的组件；为"否"时，刷新后回到子应用初始路由。
- **直接 URL 访问**：能否通过直接输入或分享 URL 直接打开子应用的某个具体页面。为"是"时，URL 中包含完整的子应用路由信息，任何用户拿到该 URL 都能访问到对应页面；为"否"时，URL 中不包含子应用路由信息，直接访问只能进入子应用首页。

**方案3中"需额外处理"的具体说明：**

- **页面刷新保持**：BrowserRouter 依赖 `popstate` 事件恢复路由状态，但 HashRouter 下 `popstate` 不携带 hash 路由变化。刷新后子应用 mount 时，需要从 `location.hash` 中解析出当前子应用路由路径，通过 qiankun props 或全局变量传递给子应用，子应用据此初始化 BrowserRouter 的初始 location，而非依赖浏览器自动恢复。
- **直接 URL 访问**：主应用需要在 `registerMicroApps` 的 `activeRule` 匹配逻辑中解析 URL hash 中的子应用路径。当用户直接访问 `http://host/#/qiankun/sub-b/detail/123` 时，主应用识别出子应用路由 `/detail/123`，通过 props 传递给子应用，子应用 mount 时据此设置 BrowserRouter 的初始路径，从而正确渲染目标页面。

---

### 12.3 HashRouter 与 BrowserRouter 的区别及切换影响

**问题背景：** HashRouter 和 BrowserRouter 有什么区别。如果子应用之前使用 BrowserRouter，要嵌到主应用中，能否直接修改为 HashRouter，修改后会出现什么影响。

#### HashRouter 与 BrowserRouter 的区别

| 对比维度 | HashRouter | BrowserRouter |
|---------|-----------|--------------|
| URL 形式 | `http://host/#/path` | `http://host/path` |
| 路由数据来源 | `location.hash` | `location.pathname` |
| 服务器配置 | 不需要（hash 不发送到服务器） | 需要 `historyApiFallback` 配置 |
| 刷新行为 | 正常（服务器返回 index.html，hash 由前端处理） | 需服务器配合，否则 404 |
| SEO | 不友好（搜索引擎可能忽略 hash 部分） | 友好 |
| History API | 使用 `hashchange` 事件 | 使用 `pushState`/`replaceState` + `popstate` 事件 |

**字段详细说明：**

- **服务器配置**：HashRouter 的路由信息存在于 URL 的 `#` 之后（hash 部分），根据 HTTP 规范，hash 不会发送到服务器，因此服务器始终收到相同的 pathname（如 `/`），无需额外配置。BrowserRouter 的路由信息存在于 pathname 中（如 `/detail/123`），服务器收到该请求后需要配置 `historyApiFallback`（开发环境）或 Nginx try_files（生产环境），将所有非静态资源请求统一返回 `index.html`，由前端路由接管，否则服务器在对应路径下找不到资源会返回 404。
- **刷新行为**：HashRouter 刷新时，浏览器发送的请求 pathname 始终为 `/`（hash 被忽略），服务器正常返回 `index.html`，前端 JS 加载后从 `location.hash` 读取路由并渲染对应页面，因此刷新行为天然正常。BrowserRouter 刷新时，浏览器将完整 pathname（如 `/detail/123`）发送给服务器，如果服务器未做 fallback 配置，会在该路径下查找资源，找不到则返回 404 页面。
- **SEO**：HashRouter 的 URL 中 `#` 之后的内容被搜索引擎视为页面内锚点，搜索引擎可能忽略 hash 部分，导致不同路由的页面被视为同一个页面，无法被分别索引。BrowserRouter 的 URL 是标准的路径结构，搜索引擎可以将不同 URL 作为不同页面进行抓取和索引，有利于 SEO。
- **History API**：HashRouter 通过监听 `hashchange` 事件来感知路由变化，导航操作通过修改 `location.hash` 实现，浏览器会自动将其加入历史栈。BrowserRouter 通过 `history.pushState()` / `history.replaceState()` 修改 URL 的 pathname 部分，并通过 `popstate` 事件感知浏览器前进/后退操作。需要注意的是，`pushState` 和 `replaceState` 不会触发 `popstate` 事件，只有浏览器的前进/后退按钮才会触发。

#### 能否直接从 BrowserRouter 修改为 HashRouter

可以修改，但会出现以下影响：

1. **URL 结构变化**：所有路由 URL 从 `/detail/123` 变为 `/#/detail/123`，已有的外部链接、书签全部失效
2. **与主应用 hash 路由冲突**：主应用 HashRouter 已占用 hash（如 `#/qiankun/sub-b`），子应用也用 HashRouter 会争抢同一个 hash 空间。React 可通过 `basename` 从 hash 中剥离前缀解决，但 Vue 的 `createWebHashHistory` 无法做到
3. **路由参数获取**：从 `location.pathname` 改为从 `location.hash` 解析，原有的 `useParams`、`useSearchParams` 等仍可工作（React Router 内部处理），但第三方库直接读取 `location.pathname` 的会失效

#### 不修改 Router 类型嵌入子应用的方法

子应用使用 BrowserRouter，主应用使用 HashRouter，根本冲突在于 BrowserRouter 从 `location.pathname` 读取路由，而 HashRouter 下 pathname 恒为 `/`。在不修改双方 Router 类型的前提下，可通过以下方法实现嵌入：

**方法1：自定义 history 适配层（详细方案）**

`<BrowserRouter>` 本质上是 `<Router>` + `createBrowserHistory()` 的封装。自定义 history 适配层的思路是：创建一个自定义 history 对象，从 `window.location.hash` 中解析出 pathname 和 search，伪装成 browser history 的行为，让 React Router 的路由逻辑正常工作。

**URL 结构映射关系：**

主应用 HashRouter 下，浏览器 URL 为：
```
http://localhost.uat.com:8080/#/qiankun/sub-b/detail/123?foo=bar
```

需要映射为子应用 BrowserRouter 能理解的 location：
```
pathname: /detail/123
search: ?foo=bar
```

映射过程：从 `location.hash`（`#/qiankun/sub-b/detail/123?foo=bar`）中剥离主应用前缀 `/qiankun/sub-b`，剩余部分 `/detail/123?foo=bar` 拆分为 pathname 和 search。

**工作原理流程图：**

```
浏览器 URL: http://host:8080/#/qiankun/sub-b/detail/123?foo=bar
                                    ↑
                          location.hash = "#/qiankun/sub-b/detail/123?foo=bar"
                                    │
                          ┌─────────▼──────────┐
                          │  自定义 history 适配层 │
                          │                    │
                          │  1. 读取 hash       │
                          │  2. 剥离 basename   │
                          │  3. 拆分 pathname/search │
                          │  4. 返回 location 对象  │
                          └────────┬───────────┘
                                   │
                          location = {
                            pathname: "/detail/123",
                            search: "?foo=bar",
                            hash: ""
                          }
                                   │
                          ┌────────▼───────────┐
                          │  React Router v6    │
                          │  (BrowserRouter语义) │
                          │                    │
                          │  匹配 /detail/:id   │
                          │  useParams() => {id: "123"} │
                          │  useSearchParams() => {foo: "bar"} │
                          └────────────────────┘
```

反向操作（子应用内部导航时）：子应用调用 `history.push('/detail/123?foo=bar')`，适配层将其转换为 hash 格式 `#/qiankun/sub-b/detail/123?foo=bar`，更新 `window.location.hash`。

**实现步骤：**

第1步：创建自定义 history 对象

```js
// hashBridgeHistory.js
import { createBrowserHistory } from 'history';

/**
 * 创建 hash 桥接 history 对象
 * 从 location.hash 中解析路由信息，使 BrowserRouter 能在 HashRouter 主应用中工作
 * @param {string} basename - 子应用的基础路径，如 '/qiankun/sub-b'
 * @returns {History} 自定义 history 对象，接口与 createBrowserHistory 返回值一致
 */
function createHashBridgeHistory(basename) {
  // 创建底层 browser history，仅用于获取 listen 的基础能力
  const browserHistory = createBrowserHistory();

  /**
   * 从 location.hash 中解析出子应用的 location 对象
   * @returns {Object} location 对象 { pathname, search, hash, state, key }
   */
  function parseHashLocation() {
    // 获取当前 hash，如 "#/qiankun/sub-b/detail/123?foo=bar"
    const hash = window.location.hash || '#/';

    // 去掉 # 前缀
    const hashPath = hash.slice(1);

    // 剥离 basename 前缀，得到子应用路由部分
    // 如 "/qiankun/sub-b/detail/123?foo=bar" -> "/detail/123?foo=bar"
    let appPath = hashPath;
    if (hashPath.startsWith(basename)) {
      appPath = hashPath.slice(basename.length);
    }

    // 如果剥离后为空，则为根路径
    if (!appPath || appPath === '') {
      appPath = '/';
    }

    // 拆分 pathname 和 search
    const searchIndex = appPath.indexOf('?');
    let pathname = appPath;
    let search = '';
    if (searchIndex !== -1) {
      pathname = appPath.slice(0, searchIndex);
      search = appPath.slice(searchIndex);
    }

    return {
      pathname,
      search,
      hash: '',
      state: browserHistory.location.state,
      key: browserHistory.location.key,
    };
  }

  // 自定义 history 对象
  const customHistory = {
    // 当前 location（从 hash 解析）
    get location() {
      return parseHashLocation();
    },
    // 监听路由变化（监听 hashchange 事件）
    listen(callback) {
      return browserHistory.listen(() => {
        callback({ location: parseHashLocation(), action: 'POP' });
      });
    },
    // 导航：将子应用路径转换为完整 hash 路径后更新
    push(to) {
      const path = typeof to === 'string' ? to : to.pathname;
      const fullPath = basename + path;
      window.location.hash = '#' + fullPath;
    },
    // 替换：与 push 类似但使用 replaceState
    replace(to) {
      const path = typeof to === 'string' ? to : to.pathname;
      const fullPath = basename + path;
      const url = window.location.origin + window.location.pathname + '#' + fullPath;
      window.history.replaceState(null, '', url);
    },
    createHref(to) {
      const path = typeof to === 'string' ? to : to.pathname;
      return '#' + basename + path;
    },
    action: 'POP',
  };

  return customHistory;
}

export default createHashBridgeHistory;
```

第2步：子应用入口文件使用自定义 history

```jsx
// main.jsx（子应用入口）
import { unstable_HistoryRouter } from 'react-router-dom';
import createHashBridgeHistory from './hashBridgeHistory';

/**
 * 渲染子应用到挂载节点
 * @param {Object} props qiankun 注入的参数对象（可含 container 等）
 */
function render(props) {
  // 解构出挂载容器
  const { container } = props || {};
  // 被主应用加载时用 container 内的 #root，独立运行时用 document 的 #root
  const mountNode = container ? container.querySelector('#root') : document.getElementById('root');
  const isQiankun = window.__POWERED_BY_QIANKUN__;

  if (isQiankun) {
    // qiankun 环境：使用自定义 hash 桥接 history
    // Router 类型不变（仍是 browser history 语义），仅改变 location 数据来源
    const history = createHashBridgeHistory('/qiankun/sub-b');
    root = createRoot(mountNode);
    root.render(
      <unstable_HistoryRouter history={history}>
        <App />
      </unstable_HistoryRouter>
    );
  } else {
    // 独立运行：正常使用 BrowserRouter
    root = createRoot(mountNode);
    root.render(
      <BrowserRouter basename="/">
        <App />
      </BrowserRouter>
    );
  }
}
```

第3步：子应用路由定义保持不变

```jsx
// App.jsx - 路由定义无需任何修改
function App() {
  return (
    <Routes>
      <Route path="/" element={<List />} />
      <Route path="/detail/:id" element={<Detail />} />
      <Route path="/edit" element={<Edit />} />
    </Routes>
  );
}
```

子应用内部的 `useParams()`、`useSearchParams()`、`<Link to="/detail/123">` 等全部正常工作，因为 React Router 看到的 location 对象的 pathname 和 search 与独立运行时一致。

**数据流完整路径：**

以用户点击子应用内 `<Link to="/detail/123?foo=bar">` 为例：

```
1. 用户点击 <Link to="/detail/123?foo=bar">
   │
2. React Router 调用 history.push('/detail/123?foo=bar')
   │
3. 自定义 history.push() 拦截
   ├── 拼接: '/qiankun/sub-b' + '/detail/123?foo=bar'
   ├── 更新: window.location.hash = '#/qiankun/sub-b/detail/123?foo=bar'
   └── 触发 hashchange 事件
   │
4. hashchange 事件触发 listen 回调
   │
5. parseHashLocation() 重新解析
   ├── hash: '#/qiankun/sub-b/detail/123?foo=bar'
   ├── 剥离 basename: '/detail/123?foo=bar'
   ├── pathname: '/detail/123'
   └── search: '?foo=bar'
   │
6. React Router 收到新 location，匹配路由 /detail/:id
   │
7. <Detail /> 组件渲染
   ├── useParams() => { id: '123' }
   └── useSearchParams() => { foo: 'bar' }
```

#### MemoryRouter 的路由参数问题

子应用在 qiankun 环境下使用 MemoryRouter，独立运行使用 BrowserRouter，存在以下路由参数获取问题：

**1. 页面刷新丢失内部路由状态**

MemoryRouter 的路由历史保存在 JavaScript 内存中，不写入 URL。当用户在子应用内部导航到 `/detail/123` 后刷新页面，MemoryRouter 会重新初始化，回到初始路由，`/detail/123` 的状态丢失。用户无法通过刷新保持当前页面。

**2. 直接访问 / 书签 / 分享链接失效**

MemoryRouter 下子应用内部的路由不反映在浏览器 URL 中，用户无法直接通过 URL 访问子应用的某个具体页面。例如无法通过 `http://host/#/qiankun/sub-b/detail/123` 直接打开详情页，刷新后只会回到子应用首页。

**3. 浏览器前进 / 后退按钮异常**

MemoryRouter 维护自己的内存历史栈，与浏览器的历史栈不同步。浏览器的前进/后退按钮不会触发子应用内部的路由变化，用户体验不一致。

**4. 查询参数获取问题**

主应用通过 hash URL 传递查询参数（如 `#/qiankun/sub-b?source=main-app&userId=1001`），BrowserRouter 环境下子应用可从 `location.search` 读取。但 MemoryRouter 下 `location.search` 为空，需要从 qiankun props 或手动解析 hash 中提取参数。

**结论：** MemoryRouter 适合子应用内部路由层级浅、不需要直接 URL 访问的场景。对于需要保持 URL 状态、支持刷新和分享的子应用，MemoryRouter 存在实际问题，应优先考虑自定义 history 适配层方案。

#### 方案优缺点对比

| 对比维度 | MemoryRouter 方案 | 自定义 history 适配层方案 |
|---------|-------------------|--------------------------|
| 实现复杂度 | 低（直接替换组件） | 中（需实现自定义 history） |
| URL 反映子应用路由 | 否（内存中，不可见） | 是（反映在 hash 中） |
| 页面刷新保持路由 | 否（回到初始页） | 是（从 hash 重新解析） |
| 直接 URL 访问 | 否 | 是 |
| 浏览器前进/后退 | 不同步（内存栈与浏览器栈不一致） | 同步（hash 变化驱动浏览器历史） |
| 分享链接 | 否 | 是 |
| 查询参数 | 需手动从 props/hash 提取 | 自动解析（useSearchParams 正常工作） |
| 子应用代码改动 | 需切换 Router 组件 | 入口文件替换 history 即可，路由定义不变 |

---

### 12.4 UMD 打包格式的影响

**问题背景：** 子工程使用 webpack 打包时，添加打包产物导出为 UMD 格式，这个格式有什么影响，对之前没有添加 qiankun 子工程配置时的打包产物又有什么影响。

UMD（Universal Module Definition）格式不影响打包产物的运行逻辑。UMD 只是模块定义格式的包装层，它在不改变代码执行逻辑的前提下，将入口文件的 `exports` 挂载到全局变量 `window['sub-app-c']` 上，同时兼容 CommonJS 和 AMD 环境。

对比未添加 qiankun 配置时的普通打包产物：

| 对比维度 | 未添加 qiankun 配置 | 添加 UMD 配置后 |
|---------|--------------------|-----------------|
| 代码执行逻辑 | 不受影响 | 不受影响 |
| 模块导出方式 | IIFE，不暴露全局变量 | 入口 exports 挂载到 `window['sub-app-c']` |
| 独立运行 | 正常 | 正常（额外暴露的全局变量不影响功能） |
| chunk 加载全局变量 | 默认 `webpackJsonp` | 改为唯一名称 `webpackJsonp_sub_app_c` |

`chunkLoadingGlobal` 从默认的 `webpackJsonp` 改为唯一名称，仅影响 webpack 按需加载 chunk 时使用的全局队列变量名，不影响功能，但防止多个 webpack 子应用共存时队列互相覆盖。

**UMD 格式对最终打包产物格式的影响：**

UMD 配置不会影响 webpack 最终打出的产物格式。这里需要区分两个概念：

- **模块定义格式**：由 webpack 的 `output.library.type` 配置决定（如 `umd`、`commonjs2`、`var` 等），影响的是入口 JS 文件内部的模块包装方式。
- **最终部署产物格式**：如 zip、tar.gz 等压缩包格式，由部署脚本或 CI/CD 流程决定，与 webpack 的打包配置完全无关。

webpack 的打包产物始终是静态资源文件（JS、CSS、HTML 等），是否配置 UMD 只改变这些 JS 文件内部的模块导出方式，不改变文件本身的类型和数量。最终将这些静态资源打包成 zip 还是 tar.gz，是部署阶段由构建脚本（如 `tar`、`zip` 命令或 CI 插件）完成的，与 webpack 无关。

也就是说，之前打包打出来的产物是 zip 包，添加 UMD 配置后打出来的仍然是 zip 包，产物格式不会改变。

**output.library.type 配置项详解：**

上文提到 `output.library.type` 决定模块定义格式，这里展开说明该配置在未添加 qiankun 配置时的默认值、所有取值及其区别。

**未添加 qiankun 配置时的默认值：**

`output.library.type` 的默认值是 `'var'`，但当 `output.library.name` 未设置时，该默认值实际不生效。以主应用配置为例：

```js
// main-app/webpack.config.js -- 完全没有 output.library 配置
output: {
  path: path.resolve(__dirname, 'dist'),
  filename: 'bundle.js',
  publicPath: '/'
}
```

此时 webpack 的行为是：

- `output.library` 整个对象未配置（没有 `name`），webpack **不会生成任何模块导出包装代码**。
- 打包产物是一个匿名 bundle，入口代码直接执行，`exports` 不挂载到任何地方（不挂 `window`、不挂 `module.exports`、不走 AMD）。
- 这是普通 Web 应用的默认行为：应用只管自己执行渲染，不需要把导出暴露给外部。

对比子应用 C 的配置：

```js
// sub-app-c/webpack.config.js -- 显式配置了 library
output: {
  // 模块名称，挂载到 window 上时的属性名
  library: 'sub-app-c',
  // 模块类型（webpack4 写法，webpack5 为 library.type）
  libraryTarget: 'umd',
  // 避免与其它子应用的 chunk 加载全局变量冲突
  chunkLoadingGlobal: 'webpackJsonp_sub_app_c'
}
```

所以"未添加 qiankun 配置"和"添加 UMD 配置后"的核心区别在于：**前者根本不暴露导出，后者把入口 exports 挂载到 `window['sub-app-c']` 上**。这就是本节前面对比表格中"IIFE，不暴露全局变量"与"入口 exports 挂载到 window"的区别来源。

**output.library.type 的所有取值：**

webpack 5 支持的 `output.library.type` 取值（webpack 4 中对应 `output.libraryTarget`）：

| 类型 | 挂载目标 | 适用环境 |
|------|---------|---------|
| `var`（默认） | `var LibraryName = ...`（全局变量声明） | 浏览器 |
| `assign` | `LibraryName = ...`（赋值给已有全局变量，不声明） | 浏览器 |
| `this` | `this.LibraryName = ...` | 浏览器 / Node |
| `window` | `window.LibraryName = ...` | 浏览器 |
| `self` | `self.LibraryName = ...` | 浏览器 / Web Worker |
| `global` | `global.LibraryName = ...` | Node.js |
| `commonjs` | `exports.LibraryName = ...` | CommonJS |
| `commonjs2` | `module.exports = ...` | CommonJS（Node） |
| `amd` | `define('LibraryName', [], ...)` | AMD |
| `umd` | 兼容 CommonJS + AMD + 全局变量 | 通用 |
| `system` | SystemJS register 格式 | SystemJS |
| `jsonp` | 包裹在 jsonp 回调中 | 浏览器 |
| `module` | ES Module（`export`） | ESM（需开启实验特性） |

**各取值的区别：**

核心区别在于：打包产物的入口 JS 文件用什么模块包装方式把 `exports` 暴露出去。代码执行逻辑不变，变的是外层的包装器。按场景分三类说明：

**① 全局变量类（`var` / `assign` / `window` / `self` / `global` / `this`）**

把导出挂到某个全局对象上，区别只是挂载的目标不同：

```js
// type: 'var' -- 声明全局变量
var subAppC = (function() { return exports; })();

// type: 'window' -- 挂载到 window 对象
window.subAppC = (function() { return exports; })();

// type: 'global' -- 挂载到 global 对象（Node 环境）
global.subAppC = (function() { return exports; })();
```

- 只能在对应环境使用（`window` 类浏览器专属，`global` 类 Node 专属）。
- qiankun 早期文档曾推荐 `libraryTarget: 'window'`，但现在统一推荐 `umd`，因为 UMD 兼容性更好。

**② 模块规范类（`commonjs` / `commonjs2` / `amd` / `module`）**

按特定模块规范包装，需要对应环境支持：

```js
// type: 'commonjs2' -- 整体替换 module.exports
module.exports = (function() { return exports; })();

// type: 'amd' -- AMD define 包装
define('sub-app-c', [], function() { return exports; });
```

- `commonjs` 只能挂到 `exports.xxx`，`commonjs2` 可以整体替换 `module.exports = ...`（更彻底）。
- `module` 是 ES Module 格式，输出 `export default`，需要 webpack 5 + 实验特性。

**③ 通用兼容类（`umd`）-- qiankun 的选择**

UMD 会智能判断当前环境，自动选择挂载方式：

```js
// type: 'umd' 的包装结构（简化）
(function webpackUniversalModuleDefinition(root, factory) {
  if (typeof exports === 'object' && typeof module === 'object')
    module.exports = factory();        // CommonJS 环境
  else if (typeof define === 'function' && define.amd)
    define([], factory);               // AMD 环境
  else if (typeof exports === 'object')
    exports['sub-app-c'] = factory();  // CommonJS2
  else
    root['sub-app-c'] = factory();     // 全局变量（浏览器挂到 window）
})(this, function() { return exports; });
```

UMD 同时兼容 CommonJS、AMD 和全局变量三种环境，无论子应用 JS 被谁加载执行，导出都能被正确挂载。

**为什么 qiankun 必须用 `umd`：**

qiankun 通过 `import-html-entry` 在沙箱代理环境下执行子应用 JS：

- 用 `commonjs2`：沙箱里没有真实的 `module.exports`，会失败。
- 用 `window`：写死全局变量，qiankun 的 JS 沙箱会拦截 `window` 访问，虽然能工作但耦合死。
- 用 `umd`：factory 函数接收到的 `root` 是 qiankun 沙箱代理对象，`root['sub-app-c'] = factory()` 能把生命周期函数挂到沙箱上，主应用再从沙箱读取 `bootstrap/mount/unmount`。这就是 qiankun 推荐用 `umd` 的根本原因--它在任何宿主环境下都能正确暴露导出，且配合 JS 沙箱最自然。

**webpack 与部署脚本/CI 的职责划分：**

两者是构建流程中的不同阶段，各司其职：

| 阶段 | 负责方 | 主要职责 |
|------|--------|---------|
| 源码编译打包 | webpack | 将源代码（JS/TS/JSX/Vue 等）编译为浏览器可执行的静态资源 |
| 产物收集压缩 | 部署脚本/CI | 将 webpack 输出的 `dist` 目录收集并压缩为 zip/tar.gz 等部署包 |
| 上传部署 | 部署脚本/CI | 将部署包上传到服务器/CDN，完成发布 |

具体来说：

- **webpack 做的事**：模块解析与打包、代码分割（chunk）、资源处理（图片/字体等）、模块格式包装（通过 `output.library.type` 决定 UMD/CommonJS/var 等导出方式）、代码压缩混淆。产出物是 `dist` 目录下的静态资源文件（JS、CSS、HTML 等）。
- **部署脚本/CI 做的事**：触发构建命令（如 `npm run build`）、注入环境变量、将 `dist` 目录压缩为 zip/tar.gz 等格式、将压缩包上传到服务器或 CDN、执行服务重启或缓存刷新。

两者的边界在于：webpack 的产出始终是 `dist` 目录下的静态资源文件，压缩包格式是部署脚本在拿到这些静态资源后额外做的打包动作。因此 UMD 配置只影响 webpack 阶段的 JS 文件内部模块包装方式，不会跨越到部署脚本阶段去改变压缩包格式。

**结论：** UMD 格式不影响最后打包产物的运行，只是 UMD 格式能将入口文件的 exports 挂到 window 上而已，供 qiankun 的 `import-html-entry` 拉取 JS 执行后读取生命周期函数。

---

### 12.5 部署后不同域名的跨域问题

**问题背景：** 当前在 qiankunProject 文件内，项目都是在 localhost 中运行，当主应用和子应用都部署后，如果域名不同，会出现跨域问题吗，主应用该如何访问子应用资源。

会产生跨域。主应用需要通过 `fetch` 拉取子应用的 entry HTML 和 JS 资源，当主应用和子应用域名不同时，浏览器同源策略会阻止跨域请求。

**开发环境的解决方式：**

| 构建工具 | 跨域配置 |
|---------|---------|
| Webpack | `devServer.headers: { 'Access-Control-Allow-Origin': '*' }` |
| Vite | `server.cors: true` |

**生产环境的解决方式：**

1. **子应用服务器配置 CORS**：在子应用的 Nginx 或服务器配置中，添加 CORS 响应头，允许主应用域名访问：
   ```
   # Nginx 配置示例
   add_header Access-Control-Allow-Origin "https://main-app.example.com";
   add_header Access-Control-Allow-Methods "GET, OPTIONS";
   ```

2. **Nginx 反向代理（推荐）**：通过 Nginx 反向代理，将子应用资源代理到主应用域名下的某个路径，浏览器看到所有请求来自同一域名，避免跨域：
   ```
   # Nginx 配置示例
   location /sub-app-b/ {
       proxy_pass http://sub-app-b-server:5174/;
   }
   ```
   主应用的 `entry` 配置改为 `//main-app.example.com/sub-app-b`。

---

### 12.6 bootstrap 和 mount 生命周期函数的触发顺序

**问题背景：** 子应用中的 bootstrap 和 mount 生命周期函数，哪个先触发。

**bootstrap 先触发。**

qiankun 的生命周期调用顺序如下：

| 阶段 | 调用时机 | 调用次数 | 作用 |
|------|---------|---------|------|
| `bootstrap` | 子应用**首次**被激活时调用 | 整个生命周期仅一次 | 初始化全局配置，通常只做 console 或空操作 |
| `mount` | 每次**进入**子应用路由时调用 | 每次进入都调用 | 创建应用实例并渲染到 container 内的 DOM 节点 |
| `unmount` | 每次**离开**子应用路由时调用 | 每次离开都调用 | 销毁应用实例，清理 DOM 和事件，释放内存 |

**首次进入子应用的执行流程：**

```
用户首次访问 /qiankun/sub-b
    │
    ├── qiankun 匹配 activeRule
    ├── fetch 子应用 entry HTML
    ├── 解析 HTML，加载 JS 资源
    ├── 执行子应用 JS，注册生命周期函数
    │
    ├── 调用 bootstrap()    ← 首次且仅此一次
    ├── 调用 mount(props)   ← 每次进入都调用
    └── 子应用渲染到 #sub-app-viewport
```

**后续再次进入子应用的执行流程：**

```
用户离开 /qiankun/sub-b，再次访问 /qiankun/sub-b
    │
    ├── 离开时调用 unmount(props)
    ├── 再次进入时 qiankun 匹配 activeRule
    │
    ├── 跳过 bootstrap()    ← 不再调用（仅首次一次）
    ├── 调用 mount(props)   ← 每次进入都调用
    └── 子应用渲染到 #sub-app-viewport
```
