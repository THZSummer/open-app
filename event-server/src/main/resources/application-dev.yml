spring:
  # Redis 配置（开发环境）
  data:
    redis:
      host: localhost
      port: 6379
      database: 0

# API Server 配置
api-server:
  url: http://localhost:18081

# 日志级别
logging:
  level:
    com.xxx.event: DEBUG
