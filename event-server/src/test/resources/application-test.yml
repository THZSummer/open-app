spring:
  # Redis 配置（测试环境 - 使用嵌入式 Redis）
  data:
    redis:
      host: localhost
      port: 6379
      database: 1

# API Server 配置
api-server:
  url: http://localhost:18081

# 日志级别
logging:
  level:
    com.xxx.event: DEBUG
